# @bsuite/schema-registry — docs index

Documentation for the `@bsuite/schema-registry` package (tenant schema, page
layout, navigation hooks, and the client-agnostic schema-builder service for
BSuite consumer apps).

Naming convention: `YYYYMMDD-descriptive-name-type-vMAJOR.MINOR[STATUS].md`
(status codes: W=Working, D=Draft, R=Review, A=Approved, F=Frozen).

## Index

- [20260723-schema-registry-builder-service-feature-v1.00W.md](./20260723-schema-registry-builder-service-feature-v1.00W.md)
  — 0.4.0 `createSchemaBuilderService` factory + fail-open `useFeatureEnabled`
  hook: API, semantics, the `r8`/`r80` `AppScope` note, and the consumer
  migration path.
