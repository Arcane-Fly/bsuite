# BSuite — Windsurf Cascade Rules

## Agent Skills

Before starting any task in this workspace, read and apply relevant skills from `/home/braden/.agents/skills/`.

**Start every task by reading:** `/home/braden/.agents/skills/master-orchestration/SKILL.md`

### Required Skills by Project

#### All Projects (React + Vite + Supabase)
- `/home/braden/.agents/skills/vercel-react-best-practices/SKILL.md` — 40+ React perf rules from Vercel Engineering
- `/home/braden/.agents/skills/vercel-composition-patterns/SKILL.md` — Compound components, state lifting, composition patterns
- `/home/braden/.agents/skills/vercel-web-design-guidelines/SKILL.md` — 100+ accessibility, UX, performance rules
- `/home/braden/.agents/skills/bsuite-brand-system/SKILL.md` — D2C Neon Electric vs Corporate branding (NEVER mix)

#### conduit (Next.js 16 App Router)
- `/home/braden/.agents/skills/vercel-next-best-practices/SKILL.md` — RSC boundaries, data patterns, file conventions, async APIs
- `/home/braden/.agents/skills/vercel-next-cache-components/SKILL.md` — PPR, `use cache`, cacheLife, cacheTag
- `/home/braden/.agents/skills/vercel-next-upgrade/SKILL.md` — Migration guides and codemods

#### conduit + crm7 (AI SDK)
- `/home/braden/.agents/skills/vercel-ai-sdk/SKILL.md` — generateText, streamText, useChat, tools, structured output

#### mobile (React Native / Expo)
- `/home/braden/.agents/skills/vercel-react-native-skills/SKILL.md` — Performance, animations, gestures, platform patterns

### Mandatory Workflow Skills
- `/home/braden/.agents/skills/brainstorming/SKILL.md` — MANDATORY before any new feature or creative work
- `/home/braden/.agents/skills/best-practice-research/SKILL.md` — Research official docs before implementing
- `/home/braden/.agents/skills/test-driven-development/SKILL.md` — Write tests before code
- `/home/braden/.agents/skills/code-quality-enforcement/SKILL.md` — DRY, naming, architecture
- `/home/braden/.agents/skills/security-audit/SKILL.md` — Secrets, injection, XSS
- `/home/braden/.agents/skills/qa-and-verification/SKILL.md` — Prove changes work
- `/home/braden/.agents/skills/verification-before-completion/SKILL.md` — NEVER claim done without evidence
- `/home/braden/.agents/skills/multi-agent-red-team-implementation/SKILL.md` — Red-team all implementations

## Project Reference

See AGENTS.md at project root for full project overview, quality standards, and per-project notes.

## Tech Stack Summary

| Project | Framework | Key Tech |
|---------|-----------|----------|
| business-suite-unified | React + Vite | Stripe, Radix UI |
| crm7 | React + Vite | AI SDK, PWA, sql.js |
| conduit | Next.js 16 | AI SDK, TanStack Query, SSR |
| braden | React + Vite | Corporate branding, CSP |
| R80.3 | React + Vite | PWA, PDF export, Fair Work API |
| mobile | Expo / React Native | Camera, biometrics, notifications |

## AI Model Configuration (Vercel AI Gateway)

CRM7 and Conduit use Vercel AI Gateway for model routing. **Do NOT revert these models.**

| Role | Model ID | Gateway Key |
|------|----------|-------------|
| **Default** | `xai/grok-4.1-fast-reasoning` | `grok-4.1-fast` |
| **Fallback** | `anthropic/claude-sonnet-4.6` | `claude-sonnet-4.6` |
| **Complex** | `anthropic/claude-opus-4.6` | `claude-opus-4.6` |

- Gateway URL: https://vercel.com/braden-pty-ltd/crm7/ai-gateway/models/grok-4.1-fast-reasoning
- Config source of truth: `crm7/src/lib/ai/config.ts`
- Model router: `crm7/src/lib/ai/model-router.ts`
- **NEVER replace grok-4.1-fast-reasoning as default** — it is the configured AI Gateway model

## Critical Rules

- TypeScript strict mode everywhere
- pnpm as package manager (never npm/yarn)
- Vitest for Vite projects, Jest for conduit (Next.js)
- D2C Neon Electric theme for all projects EXCEPT braden (uses Corporate branding)
- Never commit API keys or secrets
- Conventional commits: `type(scope): description`
- @bsuite/* packages are on npm — NEVER use `workspace:*` in consumer projects

### pnpm Lockfile Generation

**NEVER run `pnpm install` from within the bsuite directory tree** when updating a project's lockfile. The bsuite `pnpm-workspace.yaml` (scoped to `packages/*`) causes pnpm to embed workspace-relative paths (`..`) into the lockfile, breaking Vercel with `ERR_PNPM_OUTDATED_LOCKFILE`.

Always regenerate from an isolated directory **outside** the bsuite tree:

```bash
mkdir ~/crm7_lockgen && cp crm7/package.json ~/crm7_lockgen/
cd ~/crm7_lockgen && pnpm install
cp ~/crm7_lockgen/pnpm-lock.yaml crm7/pnpm-lock.yaml && rm -rf ~/crm7_lockgen
```

Correct lockfile: `.:` as only importer. Broken lockfile: `..` or `../packages/*` as importers.

## Google Cloud Authentication

**CRITICAL**: All Google API access MUST use Workload Identity Federation (WIF). Static service account keys are **BANNED**.

- **GCP Project:** `claritycrm-hpofn` (project number `111744121676`)
- **Service Account:** `firebase-adminsdk-fbsvc@claritycrm-hpofn.iam.gserviceaccount.com` (key-less — WIF only)
- **WIF Pool:** `supabase-edge-functions` (global, Supabase OIDC as IdP)
- **WIF Provider:** `supabase-auth` (trusts issuer `https://tuybltdrdefjblnplpqo.supabase.co/auth/v1`)
- **Auth flow:** Supabase JWT → Google STS token exchange → SA impersonation → short-lived access token
- **NEVER create or store service account JSON keys** — use WIF for all Google API access
- **Reference impl:** `crm7/supabase/functions/generate-document/index.ts`
- **Supabase secrets (non-sensitive metadata):** `GCP_PROJECT_NUMBER`, `GCP_WIF_POOL_ID`, `GCP_WIF_PROVIDER_ID`, `GCP_SA_EMAIL`

## Zero-Defer Policy

- Never defer fixes — fix issues in the same session
- No "TODO: implement later" for known problems
- Rate limiting, error handling, validation, tests ship with the feature
- If a fix requires user input, ask immediately — don't defer

## Authentication & OAuth

Full details in `AGENTS.md` and `docs/AUTH-MAP.md`. Key rules:

- All `.crm7.app` Supabase clients MUST use `cookieStorage` with `domain=.crm7.app`
- All Supabase clients MUST use `flowType: 'pkce'`
- Never duplicate the OAuth consent screen — BSU is the only OAuth server
- BS OAuth tokens are NOT Supabase sessions — they run in parallel

## AI Implementation Standards

- Always use `DefaultChatTransport` — NEVER `TextStreamChatTransport`
- Always use `toUIMessageStreamResponse()` — NEVER `toTextStreamResponse()`
- Always `convertToModelMessages(messages)` before `streamText()`
- `export const maxDuration = 30` on all Vercel edge AI routes
- Rate limiting mandatory on all AI endpoints
- Run `pnpm typecheck` before marking AI work complete

---

## Persistent Memory Protocol

This project uses the QIG Memory API for cross-session continuity.

**At session start — run these before any work:**

```bash
curl -s https://qig-memory-api.vercel.app/api/memory/bsuite_session_latest | jq -r '.content'
curl -s https://qig-memory-api.vercel.app/api/memory/bsuite_pending_actions | jq -r '.content'
curl -s https://qig-memory-api.vercel.app/api/memory/bsuite_decisions | jq -r '.content'
```

**Write immediately after every commit, decision, or error fix — do NOT wait for session end:**

```bash
curl -X PUT https://qig-memory-api.vercel.app/api/memory/bsuite_session_latest \
  -H "Content-Type: application/json" \
  -d '{"category":"session_summary","content":"[summary]","updated":"[ISO timestamp]"}'
```

Key naming: all bsuite keys prefixed `bsuite_`. Session keys: `bsuite_session_YYYYMMDD[a-z]`. Update `bsuite_session_latest` pointer after every write.

See `MEMORY_PROTOCOL.md` at project root for full protocol.
