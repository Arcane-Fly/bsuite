{
	"name": "bsuite",
	"version": "0.0.0",
	"private": true,
	"engines": {
		"node": "24"
	},
	"pnpm": {
		"overrides": {
			"flatted": "^3.4.2",
			"undici": "^7.24.0",
			"vite": "^6.4.2",
			"brace-expansion": "^5.0.5",
			"picomatch": "^4.0.4",
			"happy-dom": "^20.8.9",
			"yaml": "^2.8.3"
		}
	},
	"scripts": {
		"lint:all": "for dir in crm7 R80.3 braden business-suite-unified conduit; do echo \"\\n=== Linting $dir ===\"; (cd $dir && pnpm lint) || exit 1; done",
		"test:all": "for dir in crm7 R80.3 braden business-suite-unified conduit; do echo \"\\n=== Testing $dir ===\"; (cd $dir && pnpm test -- --run) || exit 1; done",
		"build:all": "for dir in crm7 R80.3 braden business-suite-unified conduit; do echo \"\\n=== Building $dir ===\"; (cd $dir && pnpm build) || exit 1; done",
		"typecheck:all": "for dir in crm7 R80.3 braden business-suite-unified conduit; do echo \"\\n=== Typechecking $dir ===\"; (cd $dir && pnpm tsc --noEmit) || exit 1; done",
		"test:packages": "pnpm -r --filter './packages/*' test -- --run"
	},
	"dependencies": {
		"@types/react": "^19.2.14",
		"@types/react-dom": "^19.2.3"
	}
}
