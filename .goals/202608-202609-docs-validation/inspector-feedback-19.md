# Inspector Feedback — Iteration 19

## Verdict: FAIL

## Acceptance Criteria Check

- [x] Authoritative inventory and arithmetic — independently replayed: `find docs docs/plans -type f \( -name '202608*' -o -name '202609*' \) -print | sort -u` returns exactly 128 paths, which are in exact bijection with the 128 sequential ledger rows (no missing path, no duplicate, no extra row). Verdict totals independently tallied from the ledger table: `5 VALIDATED-CURRENT / 7 VALIDATED-DRIFTED / 3 DUPLICATE-CLUSTER / 0 SUPERSEDED / 113 UNVERIFIABLE`, matching the ledger's own summary line.
- [x] Registry and index gates — independently replayed: `node scripts/generate-component-registry.mjs --check` → `component-registry: in sync (174 components)`; `node scripts/generate-feature-index.mjs --check` → `feature-index: in sync (661 rows)`; parsing `docs/00-roadmap/bsuite-feature-index.json` gives `{rows:661, modules:28, capabilityAreas:57}`, matching the ledger.
- [x] Focused row 81 — verified as corrected: `docs/20260826-route-surface-map-v1.00W.md` states 553 routes (title at line 14, "553 rows" at line 16, `TOTAL 553` at line 52 inside `§2 — THE LEDGER`), and current `node scripts/check-route-surface-map.mjs` independently replays to 558 inventory routes/558 mapped rows/1,157 checks/0 problems, exactly as the ledger states. The row now explicitly states the +5 drift, resolving iteration 18's finding. Minor imprecision only: the cited anchor `:1–3` is the YAML front matter (`---`, `kind: measurement`, `authority: agent`), which does not itself contain "553" — the real anchor is the title/§2 total row — but this does not change the verified fact and is not blocking.
- [x] Focused row 114 — verified: line anchors `docs/plans/20260817-estate-completion-plan-v1.00D.md:6` ("46 findings"), `:21–25` (acceptance-test/class-sweep/D-85 language), `:87` ("Default state is 'I don't know'"), `:458–503` (§4.1 RLS remediation steps, prescriptive/future-tense), and `:640–649` (G1–G7 gates table) all read exactly as characterized. Script self-tests independently replay: `check-doc-classification.mjs --self-test` → 9/9; `estate-align.mjs --self-test` → OK (24 cases); `estate-align.mjs --strict` → `dod coverage: 7/661 evaluated (1.1%)`, "no invariant violations" — the exact 7/661 figure the row cites; `audit-doc-completion.mjs --self-test` → 38/38. `scripts/estate-align.mjs:125-146` (sibling-class closure check) and `:301-343` (operator-ask reconciliation/cross-cutting classification) are real, relevant functions, not arbitrary anchors.
- [x] Focused row 116 — verified: line anchors `docs/plans/20260827-estate-lifecycle-process-refined-v1.00D.md:5-28` (enforcement dependency table: `estate-alignment.yml`, `check-doc-naming.mjs`, `check-doc-classification.mjs`, `audit-doc-completion.mjs`, `estate-align.mjs --strict`) and `:154-169` (Step 6 ARCHIVE: "Refuse unless the artifact's feature rows carry `dod_status: approved` with an evidence pointer, **or** a named superseding artifact is supplied") both read exactly as characterized. Dry-run replay: `node scripts/bsuite-notes-cycle.mjs` → `400 paragraphs, 98 images -> ...`, `+0 new, -0 removed`, dry run confirmed (no `--write`), matching the row's cited output exactly.
- [ ] Focused row 113 — FAILED acceptance criterion 6. The line anchors added this iteration (`:40-46,160-183,232,304,473-511`, `:172-175`, `:78`) are all verified genuine and accurately characterized against `docs/plans/20260814-portals-and-surface-class-remediation-v1.00D.md`. But the row also introduces a brand-new factual claim — **"A static source census found 787 portal-related source files/paths"** — with no command, no scope statement, and no output attached anywhere in the row or in the ledger's top-level "Fresh evidence captured for this pass" section (checked: `grep -n "787" docs/00-roadmap/20260904-dated-doc-validation-ledger-v1.00W.md` returns only this one row). Independent replay attempts to reproduce a "portal-related source files" count across the six app `src` trees, using several plausible scopes, do not land near 787: `grep -li 'portal'` over `.ts`/`.tsx` under the six `*/src` roots → **373**; the same but over the whole six repos (not just `src`) → **405**; total match count (`grep -c`, not `-l`) summed over the six `src` roots → **2,292**; broadened to `.ts`/`.tsx`/`.md`/`.sql` across the whole six-repo tree → **598**; matches inside the plan document itself → **29**. None of these — nor any other scope tried — reproduces 787. This is exactly the defect class flagged in iterations 6, 7, 14, 15, 17, and 18: a specific number presented as fact without an exact, replayable command is not evidence, and here it does not even survive an attempt at independent reproduction under any reasonable reading of "portal-related source files/paths."
- [x] Accepted architecture rulings — preserved (diff-confirmed untouched by this iteration's two commits, which change only rows 81/113/114/116 and `status.json`): CRM7 `BrowseDataTab` remains the bounded editable-grid winner; `EntityTableWidget` remains read-only; `RelationshipCanvas` remains interactive with persistence owned outside the canvas; page-builder retains independent child slots, version invalidation, and persisted drag/resize mechanics; role-token branding remains the accepted hierarchy.
- [x] Status, signature, and scope — verified: `status.json` overall `status` remains `building`; both iteration-19 Builder commits (`c885fe4dd57afd705105d2bec39702d6d8614da6`, `e0267fa409eb7a1c62445be1f0b6198dd75b90a1`) have good GPG signatures under key `5200D84D2CE96AED`; the cumulative diff since the iteration-18 commit (`96741c17e`) touches only `docs/00-roadmap/20260904-dated-doc-validation-ledger-v1.00W.md` and `.goals/202608-202609-docs-validation/status.json` — no `git add -A` sweep, no unrelated file touched.
- [x] No false overall completion — verified: 113 rows remain `UNVERIFIABLE` and the ledger's own summary states this plainly; `status.json` correctly remains `building`.

## Quality Gate

- Command: `node scripts/generate-component-registry.mjs --check`
- Result: PASS — `component-registry: in sync (174 components)`.
- Command: `node scripts/generate-feature-index.mjs --check`
- Result: PASS — `feature-index: in sync (661 rows)`.
- Command: `node -e "const x=require('./docs/00-roadmap/bsuite-feature-index.json'); ..."`
- Result: `{ rows: 661, modules: 28, capabilityAreas: 57 }`.
- Command: `node scripts/check-route-surface-map.mjs`
- Result: PASS — 1,157 items checked, 0 problems; 558 inventory routes, 558 mapped rows. Confirms row 81's +5 drift against the source document's 553.
- Command: `node scripts/check-doc-classification.mjs --self-test && node scripts/estate-align.mjs --self-test && node scripts/estate-align.mjs --strict && node scripts/audit-doc-completion.mjs --self-test`
- Result: 9/9; OK (24 cases); `dod coverage: 7/661 evaluated (1.1%)`, no invariant violations; 38/38 — all exactly as row 114 states.
- Command: `node scripts/bsuite-notes-cycle.mjs`
- Result: dry run, `400 paragraphs, 98 images`, `+0 new, -0 removed` — exactly as row 116 states.
- Command: `bash scripts/theme-gates.sh --quick`
- Result: FAIL — 10 passed, 1 failed; G11 reports 2 convertible inline colour styles, matching the ledger's own recorded evidence header (not a fresh regression, and row 14 already records this).
- Command (row-113 replay attempts): `grep -li 'portal'` over six-app `*/src` `.ts`/`.tsx` → 373; over whole six repos → 405; `grep -c` summed over six-app `src` → 2,292; broadened extensions over whole tree → 598; matches inside the plan doc itself → 29.
- Result: none reproduces the ledger's asserted **787**; the figure has no recorded command and does not survive independent reproduction under any tried scope.
- Additional checks: exact inventory/ledger bijection (`diff` against fresh `sort -u` inventory — empty), row-number sequence check (`diff` against `seq 1 128` — empty), duplicate-path check (`sort | uniq -d` — empty), direct reads of all cited focused-row source anchors, `git log --format='%H %G? %GK'` on both iteration-19 commits, and `git diff --stat` scope check since the iteration-18 commit.

## Issues Found

1. Row 113 states "A static source census found 787 portal-related source files/paths" as an established fact, with no command, no defined scope, and no output recorded anywhere in the row or the ledger's evidence section. Independent replay across five plausible scopes produces 373, 405, 598, 2,292, and 29 — none close to 787. This is unreplayable evidence asserted as fact, in the same row the prior Inspector iteration flagged for exactly this defect class.

## What Must Be Fixed

1. For row 113, either (a) record the exact command and full output that produces the 787 figure (file list or count, with the precise `find`/`grep` invocation and scope), so it is independently replayable, or (b) remove the unreplayable 787 claim entirely and rely on the already-verified plan-source anchors (`:40-46,160-183,232,304,473-511`, `:172-175`, `:78`) plus an explicit `E-OPEN` statement of what a genuine portal-surface census would require. Do not carry forward a specific number with no attached command.
2. Optional, non-blocking polish: correct row 81's anchor from `:1-3` (YAML front matter, which does not contain "553") to the actual location of the 553 claim (title line 14 / "553 rows" line 16 / `TOTAL 553` at line 52 inside `§2`).
3. Preserve the exact 128-row inventory, current verdict arithmetic (5/7/3/0/113), all accepted architecture rulings, rows 81/114/116's now-verified evidence, and `building` status — none of that regressed this iteration and none should be disturbed while fixing row 113.
