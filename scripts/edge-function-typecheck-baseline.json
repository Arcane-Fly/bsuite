{
  "_note": "Pre-existing failures at gate introduction (2026-08-16, P0-8a). SHRINKING RATCHET: this set may shrink and may never grow. ./classify-issue is here for a different reason from the rest \u2014 packages/jodie uses the Node NodeNext convention of importing a .ts source through a .js specifier, which Deno rejects without --sloppy-imports. That is a real Deno/Node interop defect in a deployed function, not gate noise; it is baselined rather than papered over with --sloppy-imports, which would weaken the check for all 64. ADDED 2026-08-16: crm7/tasks-sync. It was one of six edge functions DEPLOYED to production with no source in any repository (P0-8, bsuite#1955); re-homing it in crm7#1752 is what first brought it under this gate. Its 21 errors are 15x TS2339 'Property X does not exist on type never' from an untyped Supabase client \u2014 the count rose from 17 to 21 in the same PR because moving it off the esm.sh CDN onto jsr: (the dependency behind the 2026-08-01 four-function deploy failure) picked up stricter client types. These are TYPE-CHECK ONLY: Deno does not type-check at runtime, so the deployed function behaves identically either way, and no sibling function types its client generically either. Fixing them means annotating row types in a function nobody has ever been able to review, which is a change worth making on its own and not inside a re-home. This entry may shrink and must never grow. RATCHETED 2026-09-24: crm7/charge-rate-quote-dispatch and crm7/host-agreement-dispatch removed; both pass deno check after crm7#2719 (the shared EmailInvoker declared body: unknown, which supabase-js FunctionInvokeOptions rejects; SendBody lacked signer_contact_id).",
  "failing": [
    "./classify-issue",
    "braden/add-admin-user",
    "braden/ensure-guest-storage-access",
    "braden/list-hero-images",
    "business-suite-unified/feature-builder-ai",
    "business-suite-unified/jodie-bug-create",
    "business-suite-unified/rate-limit-check",
    "conduit/r7-automation-processor",
    "crm7/avetmiss-export",
    "crm7/document-encryption",
    "crm7/lead-capture",
    "crm7/report-delivery",
    "crm7/tasks-sync",
    "crm7/tga-organisation-sync",
    "crm7/tga-search",
    "crm7/tga-sync",
    "crm7/tga-units-sync",
    "crm7/xero-invoice-submit"
  ]
}
