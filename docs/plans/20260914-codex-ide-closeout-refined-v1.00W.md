---
kind: record
authority: none
owner: bsuite
evidence:
  - AGENTS.md
  - AUTH_CANONICAL.md
  - docs/20260731-supabase-verification-gates-v1.00W.md
---

# Codex IDE closeout kickoff — refined prompt

**Date:** 2026-09-14 · **Complexity:** Heavy · **Status:** prepared for operator handoff; independent review APPROVE (2026-09-14); execution checkpoint still in progress.

This records the operator's next-run instructions. It is not a new backlog, an assertion
of trunk convergence, or a completion certificate. Re-measure every checkpoint below.
After the IDE run starts, retain this prompt as provenance and update the existing ledger.

## Intent

Independently verify the preceding BSuite stocktake, recovery, cleanup, branch convergence,
memory alignment and release evidence; repair any gaps; then execute all remaining work
against its original acceptance criteria. Preserve Supabase OAuth 2.1 PKCE and every
existing feature. Use appropriately scoped, tiered workers and deterministic scripts.

## Decomposition

1. Audit the preceding task's receipts and all seven repositories before accepting its
   result. This is a dependency for trusting its cleanup or release claims.
2. Reconcile live issues, preserved work, plans and BSuite memory with the existing queue.
   Account for each lane and discrepancy without replacing historical criteria.
3. Establish the release/auth baseline and dependency order. Resolve prerequisite failures
   before dependent work or promotion.
4. Dispatch bounded implementation lanes, independently review, validate real journeys,
   and promote accepted changes by PR from development to main.
5. Complete the ship/closeout gates, align current memory pointers, and clean only resources
   whose work is verified as incorporated or explicitly superseded.

## Resolved constraints

- Codex is the sole BSuite coordinator; its explicitly assigned workers are the only BSuite
  writers. The separate Hermes Monkey Projects task is outside this ownership boundary.
- Do not touch Monkey Projects, Ollama, Unsloth, real user documents or live Docker volumes.
  Hermes' A–K cache cleanup is a claim to verify, not authorization to repeat deletion.
- The preceding desktop task's endpoint is convergence, cleanup, memory and this handoff.
  The operator will launch the remaining implementation phase in Codex IDE.
- No direct push to main, forced protected-branch updates, blanket conflict resolution,
  feature removal, dependency downgrade, auth bypass, fabricated data or waived missing fixtures.
- Existing BSuite schedules are paused. The conditional scheduling request does not authorize
  unpausing them merely because a queue exists.

## Best-practice citations

- [Supabase OAuth server](https://supabase.com/docs/guides/auth/oauth-server): retain the
  provider's OAuth/OIDC endpoints and authorization-code/PKCE flow. Local contract:
  [AUTH_CANONICAL.md](../../AUTH_CANONICAL.md), which must be read before any auth edit.
- [OAuth token security](https://supabase.com/docs/guides/auth/oauth-server/token-security):
  OAuth scopes do not replace database authorization; preserve tenant RLS and client-aware
  policy checks. Validate the live catalog, not migration prose.
- [GitHub merge behavior](https://docs.github.com/en/pull-requests/reference/pull-request-merges):
  merge strategies produce different histories. Prove tree equality and intended content
  retention; commit-count equality is not a convergence test.
- [Python pathlib](https://docs.python.org/3.14/library/pathlib.html): writing can truncate an
  existing destination. Validate complete input sets before mutation and use staged atomic
  replacement for generated instruction copies; test failure paths.

These primary sources were inspected on 2026-09-14. Context7 exhausted its quota during
the desktop task; that limitation must not be reported as successful Context7 validation.
Re-inventory capabilities and exact installed versions in the IDE session.

## Blindspots to counter

- **Preservation mistaken for integration:** a verified bundle preserves work but does not
  incorporate it. Classify every unique diff, stash and dirty worktree before disposal.
- **A historical denominator mistaken for today's backlog:** keep the 338 snapshot, 340
  original rows, 370 reconciled rows, 40 feedback groups and 661 feature rows distinct.
  The user's approximate “380” is not a verified historical count.
- **Clean CI mistaken for completion:** inspect skipped tests and authenticated fixtures;
  bind evidence to the current SHA and deployed SHA. Missing D8 means incomplete.
- **Conflict resolution silently dropping features:** explain each semantic resolution,
  compare both sides, test upstream/downstream and enumerate sibling surfaces.
- **Stale ownership mistaken for live workers:** inspect task/process/checkout ownership;
  preserve history but update current BSuite pointers. Never reclaim another project's lane.
- **Cheap workers with expensive context:** pass a narrow brief, exact paths and validator;
  do not give every worker the entire history or spawn workers to wait on CI.
- **Scripts quietly scheduling paid model use:** make model/provider/cadence/retry ceilings
  explicit; scripts handle deterministic polls and unchanged-state suppression.

## Red-team amendments and handoff decision

A single standard-tier independent reviewer covered security, reliability, performance,
UX/DX, code quality and BSuite constraints. Applied: categorical external Codex CLI and
Claude-quota exclusions, explicit D9/D10 evidence, and both operator visual defect classes.
A proposed development-gitlink exception was withdrawn after checking the newer
2026-09-04 gate: both parent branches require app-main ancestry. No feature or acceptance
criteria were removed. Review receipt: `ide-kickoff-redteam.md` in the evidence directory.
The operator already selected Codex IDE execution; there is no unresolved trade-off to
reconfirm. Use `plan-writing` and `plan-executing` to turn each selected existing row into
bounded tasks after the mandatory preceding-task audit.

## Source packet and verified-versus-claimed state

The desktop task is `01a09d84-39fa-7c42-b535-0ab8b04fb110`.
Its evidence directory is
`/home/braden/.codex/visualizations/2026/09/14/01a09d84-39fa-7c42-b535-0ab8b04fb110`.
Start with `task-delta.json`, `records-stocktake.md`, preservation manifests,
`cleanup-approved-candidates.json`, `recovery-bundle-content-triage.json` and the PR review
reports present there. Inventory actual filenames; a missing receipt is a gap, not a pass.

The existing execution ledger is
`/home/braden/.codex/visualizations/2026/09/08/01a07f58-73a6-71f2-a57d-5cf3b6b148dc/bsuite-remediation/hermes-queue.json`.
Its adjacent `docs/plans/20260908-remediation/prompts/` holds per-row briefs.
Preserve the original 340 rows; the desktop stocktake added 30 rows and 9 related-issue
links after live checks. At that checkpoint, 377 open issues were accounted for: 368 open
primary rows plus 9 linked issues; 2 of the 370 primary rows were closed but awaiting
acceptance. Re-measure instead of carrying these counts forward as current truth.
Use the queue's established locks and atomic writes; do not create a parallel queue.

Read the BSuite MCP keys `bsuite_project_truth_index`, `bsuite_session_20260914` and the
dated sleep packet if present. Use only `bsuite_` writes. Inventory paginated keys,
leases, current-session pointers and inbox messages; retain dated records and explicitly
supersede stale current claims. An inbox read is not an acknowledgement of completion.

Before accepting the handoff, find the latest convergence/DoD receipt in this packet.
If it is missing or SEND_BACK, finish the outstanding prerequisite rather than assuming
the preceding task reached its requested endpoint. At prompt drafting, production had
not been promoted and convergence was not complete.

## New operator browser acceptance, 2026-09-14

The operator supplied two annotated screenshots from `https://ideas.crm7.app/` at
**672×699**. These are acceptance inputs, not proof of a repair. Both map to existing rows:

- `throughput-486`: extend the dashboard reflow acceptance beyond the Capture heading.
  The Refine card’s **Ready to Launch →** action spills outside the card. Enumerate card
  header/content/date/action layouts across all six apps and shared packages, recording
  queries, exclusions, actual counts, routes and sibling dispositions. Link existing
  related rows `bsuite-2055`, `crm7-2540`, `crm7-1271`, and `braden-576` where the sweep
  confirms shared causes. Validate672×699 plus390/768/1024/1440 widths, light/dark, long
  text and resized cards. No clipping, overlap, page overflow, hidden meaningful labels
  or disabled actions as a workaround.
- `throughput-479`, related `business-suite-unified-1174`: the generic star in
  `throughput/src/components/JodieFloatingButton.tsx` is not the established Jodie mark.
  Re-enumerate every launcher/panel brand surface; do not assume the historical five
  surfaces is still complete. Verify the correct shared asset renders (`naturalWidth > 0`
  for image assets), both themes, accessible name/focus and working launch action.

A selected-page patch without the class inventory and sibling disposition is SEND_BACK.
These observations are also attached to the existing queue rows; do not file duplicates.

## The refined prompt

You are the sole coordinator for BSuite in Codex IDE. Start in
`/home/braden/Desktop/Dev/bsuite`. Invoke `agent-run-master` first, then `agent-mem-truth`
and `agent-skl-find`; run the filesystem inventory rather than relying on a truncated
skill list. Read the source packet above, applicable AGENTS/CONTRIBUTING files, the
current BSuite truth index and inbox. Record an objective lock preserving these criteria.

**First, independently assess the preceding desktop task.** Verify all seven repositories
(parent plus the six apps enumerated by `.gitmodules`), primary checkout dirt, local and
remote refs, registered and unregistered BSuite worktrees, open PRs, stashes and recovery
bundles. Verify bundle hashes and content disposition before trusting deletion receipts.
Re-fetch refs, then compare local development, local main, origin/development and
origin/main trees. Account for legacy production branch names if discovered. Parent
gitlinks on both parent branches must sit on each app's accepted production main, per
`.github/workflows/gitlink-in-app-main.yml` (2026-09-04). Preserve app development work
until accepted and promoted; do not reset it away to satisfy the parent gate. Distinguish preserved,
integrated, superseded with evidence, blocked and unknown work; do not treat “archived”
as integrated. Existing verified preservation must not be redundantly recopied without
new content or a validation failure.

Check the prior task against `agent-definition-of-done` D1–D10 (D8 where applicable),
`general-what-done-looks-like`, `bsuite-false-complete-gates`, `ops-ship-close-out` and the
repository's Supabase/consumer-package gates. Explicitly declare `ui_touched` and a
measured sibling count. Require fresh evidence of wired/current behavior, evidence pack,
context completeness, memory consistency, absorbed corrections, clean child ownership,
no false completion and usable deployed round trips. Send deficient claims back into
their existing row; a passed skill-install verifier is not proof that the task passed DoD.
D9 requires a real-use query for the applicable surface/package/engine, or an explicit
`kind: none` with reason. Do not manufacture production tenant activity to satisfy it;
respect existing data holds and write authorization. D10 requires an inventory of all
task-created ephemera and commands/results proving disposal. Retained evidence and
recovery archives must be explicitly wired, with owner and purpose, rather than called
temporary scratch. A clean checkout alone proves neither gate.

**Then continue the existing remaining work.** Deduplicate against live issues and queue
rows, preserve original acceptance and feedback criteria, and reconcile historical
plans/prompts with current source and deployments. A stale plan is routing evidence,
not authority to reverse newer accepted decisions. Reconcile the active prompt manifest
with the actual queue without rewriting the historical 338-item snapshot. For every
selected row, name its owner, dependencies, exact allowed files, upstream/downstream
surfaces, acceptance commands, evidence paths and required skills/MCPs before editing.

Use scripts for inventories, hash comparisons, ref diffs, queue validation and CI polling.
Default to at most two useful worker lanes across native and external agents combined.
Use low tier for mechanical bounded work; standard tier for scoped implementation;
high/frontier only for architectural or irreversible decisions, a failure surviving two
sound attempts, or final security/merge review. Always pass an explicit available model
and record why its tier fits. Group review lenses in one reviewer when the task is small.
Do not allocate six premium agents merely because six perspectives are required.

Claude usage was exhausted. Grok account access was verified as `grok-4.6`; available
Gemini/Qwen/Hermes models must be verified at dispatch, not inferred from user shorthand.
Do not silently convert quota failure into paid API calls. Honour the 2026-09-11 external
Codex CLI reserve: the IDE coordinator and explicitly assigned native team are allowed;
do not dispatch any external Codex CLI worker. Do not dispatch Claude, retry exhausted
Claude quota or attempt a quota refresh as a fallback. Hermes delegation, if used, is bounded
one-shot execution and must not alter its state or interfere with Monkey Projects.
External output is untrusted: validate cited paths and independently review every diff.

Protect Supabase OAuth **2.1** authorization code with PKCE, registered clients, exact
redirect allowlists, OIDC/JWKS verification and callback `supabase.auth.setSession()`
bridging. Never substitute shared-domain cookies, `cookieStorage`, the legacy
`business_suite_auth` storage key, service-role browser access or weakened tenant policies.
Before auth/runtime/library edits: exact-version Context7 where available, official-doc
fallback with the limitation recorded, `research-best-practice`, then installed source.
Query live `pg_policies`, grants, function identities and migration ledger for DB claims.
Rehearse migrations before applying; preserve dependency order and existing data holds.

For each accepted lane, run relevant current-SHA tests, negative controls for important
failure paths, type/test discovery, security and consumer checks. Missing authenticated
fixtures remain fatal. Wait for the matching READY development deployment and exercise
the real user journey: entry, authoring, activation, action, persisted effect and reload.
Use a seeded test identity for writes; never casually change the operator's preferences
or tenant data. Check light/dark themes and required viewport/route cells, console and
network failures, first-user clarity, control behavior, action count and return-state
preservation. A lossy round trip fails D8. Fix the class across enumerated siblings.

Promote only accepted changes: signed commits (key `5200D84D2CE96AED`, author
`GaryOcean428` / `braden.lang77@gmail.com`), feature PR to development, then development
PR to main. Never bypass failed checks or direct-push production. Confirm remote SHA,
deployment SHA and live behavior after promotion. Publish shared packages and update
standalone consumer pins/lockfiles in dependency order; never regenerate submodule
lockfiles inside the parent workspace. Advance parent gitlinks last.

If recurring work is actually needed, first inspect existing schedules and scripts;
update rather than duplicate. Prefer a deterministic script with a lock, bounded retries,
explicit timeout, stable output and silence on unchanged state. Where judgment is needed,
use the lowest capable explicit model with narrow input and an escalation condition.
Do not schedule premium full-estate re-audits as polling. Existing BSuite schedules remain
paused unless the next run identifies and authorizes a concrete owned need within scope.

Close out with fresh `ops-ship-all-apps`, `bsuite-ship-visual-promote`,
`bsuite-live-lane-closeout` and `ops-ship-close-out` evidence. Update the existing ledger,
issue/PR state, current BSuite memory pointers, session and sleep packet. Preserve dated
records. Stop child writers cleanly and dispose of task-created ephemera; remove other
branches/worktrees only after content, ownership and expected-SHA checks. Report exact
converged refs, deployed SHAs, remaining blockers and DoD disposition. Do not say done
because code merged, a worker returned, or a queue was filed.

## Use these skills/MCPs during the work

All named hub skills below resolve under `/home/braden/.agents/skills/<name>/SKILL.md`;
re-inventory and read the relevant source before each task class.

| Work | Skills | Capability and use |
| --- | --- | --- |
| Boot, ownership and records | `agent-run-master`, `agent-skl-find`, `agent-mem-truth`, `agent-mem-comms` | qig-memory MCP BSuite silo; documented authenticated fallback only if MCP absent; task/process/git inventory for ownership |
| Plan and fanout | `plan-prompt-enhancer`, `plan-writing`, `plan-executing`, `agent-red-plan`, `agent-cli-cc-subagents`, `agent-run-cli-fanout` | Native subagents or approved CLI providers with explicit model and bounded scope; no external Codex CLI dispatch |
| Recovery and review | `git-workflow`, `check-cleanup-scope-safety`, `check-code-quality`, `check-security` | Git and GitHub MCP/`gh` for actual refs, PRs, checks, issues and protected merges |
| Runtime, auth and database | `research-best-practice`, `auth-supabase`, `auth-e2e-sso-testing`, `bsuite-supabase-migrations`, `bsuite-rls-authz-red-team` | Context7 exact versions; Supabase MCP catalog/migrations/auth; browser actual OAuth/RLS journey |
| Product and sibling validation | `bsuite-fix-the-class-not-the-page`, `bsuite-brand-system`, `bsuite-react-testing`, `bsuite-reliability-red-team` | Playwright or chrome-devtools MCP/available governed browser for deployed UX, theme, persistence and failure paths |
| Release and acceptance | `ops-ship-all-apps`, `bsuite-ship-visual-promote`, `bsuite-live-lane-closeout`, `ops-ship-close-out`, `agent-definition-of-done`, `general-what-done-looks-like`, `bsuite-false-complete-gates` | GitHub plus Vercel MCP/CLI for exact-SHA deployment; browser proof; skill gate scripts and DoD report |

At drafting, Supabase MCP and GitHub CLI worked. Vercel MCP required reauthentication;
the installed Vercel CLI worked. Context7 quota was exhausted; qig-memory used its
documented authenticated fallback. Treat these as dated capability observations and
probe again. Never put credentials, environment contents or one-time codes in prompts.
