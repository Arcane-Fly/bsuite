# Orphaned test: site-visits assessment must not accept client signer identity (in attic, unowned)

https://github.com/GaryOcean428/crm7/issues/2435

Snapshot updatedAt: 2026-09-05T09:28:30Z. Open at capture; re-read live.

Found in the shared crm7 checkout on 2026-09-05 16:44 AWST, uncommitted, on no branch that anyone live claims: a 42-line addition to `src/pages/field-officers/site-visits/index.test.tsx` adding a `persistAssessmentMock` and a case named **"persists the complete assessment aggregate without accepting client signer identity"**.

Both live sessions (bsuite-7c, bsuite-7e) confirmed it is not theirs, and it is not from either of the PI's two salvaged lanes. It is preserved at `refs/attic/presnap/crm7-20260905-164438` and in the PI's evidence directory (`evidence/2026-09-05/salvage/crm7-*.unstaged.diff`).

The test name describes a real security property — the server, not the client, decides who signed an assessment — and a test for it that nobody owns is exactly the kind of work that disappears on the next submodule update. Someone should either (a) claim it and finish it (does `persistAssessment` actually refuse a client-supplied signer today? if the test was written first and fails, it may be documenting a live defect), or (b) decide it is superseded and say so here. Do not delete the attic ref until one of those has happened.

