---
kind: record
authority: none
owner: bsuite
---

# [RULING D-72] Reference layout for redesign — charge-calculator-mapd.jsx (read before rebuilding the calculation card)

https://github.com/GaryOcean428/R80.4/issues/57

Snapshot updatedAt: 2026-08-26T12:22:30Z. Open at capture; re-read live.

Operator ruling, quoted near-verbatim: "`/home/braden/Downloads/charge-calculator-mapd.jsx` is the reference layout. It is a demo, does not handle bulk creation and its values are hardcoded, but the visual arrangement and the calculation are both better than what shipped. Read it before redesigning."

This file (94KB) exists on the operator's machine and is confirmed to exist — it is a demo file, NOT in any git repository. Do not import or attach it; reference the path only. It is annotated with a data-source legend distinguishing [API] (live Fair Work MAPD data), [API→CALC] (derived), [USER] (user input), [CONFIG] (GTO business config never available from MAPD — super %, workers comp %, overheads, margin, funding milestones, billable weeks), and [CALC] (engine output). This legend pattern is part of what makes the file's structure worth reading before any redesign of the calculation card.

**Route/surface:** Calculator-wide redesign reference — not itself a code change

Directive: D-67 (2026-08-13)
Ruling: D-72 (2026-08-13)

## Acceptance criteria
- Before any redesign PR touching the main calculation card ships, the author confirms in the PR description that `/home/braden/Downloads/charge-calculator-mapd.jsx` was reviewed for its visual arrangement, calculation structure, and its [API]/[API→CALC]/[USER]/[CONFIG]/[CALC] data-source legend.
- The shipped redesign's field grouping and data-source labeling follow the same legend discipline (each field's origin is visually distinguishable as API-sourced, user input, config, or calculated), even though the demo file itself is never imported or shipped, and even though its hardcoded values and lack of bulk-creation handling are not carried over.

## Mandatory before merge
- **Validation loop:** §9.2 visual-equivalence — this is a layout/structure reference, not an output-value regression.
- **Equivalence target:** matches the reference layout structure at `/home/braden/Downloads/charge-calculator-mapd.jsx` (visual arrangement + calculation structure + data-source legend pattern), not its hardcoded values or its lack of bulk-creation handling.
- **Cross red-team:** bsuite-design-sheriff
- **Skills to load:** bsuite-page-grid-layout, bsuite-brand-system, general-dry-one-shot-architecture

---
*Filed under operator directive D-67 (2026-08-13). Filing is not addressing (D-59) — no fix is implied or claimed by this issue.*
