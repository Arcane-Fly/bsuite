# [Compliance review] 54 newly-added document categories: sensitive/expiry flags are agent judgment calls — Braden to review

https://github.com/GaryOcean428/crm7/issues/1128

Snapshot updatedAt: 2026-08-24T03:26:24Z. Open at capture; re-read live.

## Context

The onboarding-360 pilot (2026-07-14) found the Person-detail document-category picker (finding F13) covered only 26/80 real `DocumentCategory` enum values, missing 8 whole groups (54 values): Funding Documents, GTO Compliance, Field Officer Reports, WHS Incidents, Marketing & Communications, Quality Assurance, Host Employer, Legal & Governance.

This was fixed the same session by the docs-pipeline-fixer agent (WS-A report §5) — branch `fix/document-pipeline-tenant-encryption`, **commit `e6dcae36`** (local at time of filing) — which extends `src/lib/documents/documentCategories.ts` to the full 80-entry / 14-group taxonomy and replaces the plain `Select` with a searchable grouped combobox in `DocumentHub.tsx`.

## Why this needs Braden's review (self-reported judgment call, per WS-A §5)

No canonical spec defines the `sensitive`/`expires` flags for the 54 newly-added categories. The implementing agent made a best-effort judgment call, quoted verbatim from the WS-A report:

> **Judgment calls (self-reported — no canonical spec defines these flags for the 54 new categories):** `sensitive: true` chosen for `payment_confirmation`, `eligibility_evidence`, `complaints_appeals`, `mentoring_session_notes`, `intervention_plan`, `incident_report`, `witness_statement`, `investigation_report`, `photo_evidence`, `workers_comp_claim`, `medical_report`, `return_to_work_plan`, `complaint_register`, `director_declaration`, `legal_advice` (PII / medical / privileged / complainant content). `expires: true` for `asqa_registration`, `state_training_authority`, `scope_of_registration`, `insurance_certificate`, `host_agreement`, `site_risk_assessment`, `service_agreement`. Borderline call flagged for Braden: `photography_consent` left non-sensitive. Encrypting large WHS photo/video evidence has a performance cost (base64 round-trip) — flag if unacceptable.

This matters because `sensitive: true` routes an upload through the document-encryption path — getting a flag wrong either stores PII/medical/privileged data unencrypted, or needlessly encrypts high-volume WHS photo/video evidence with a real performance cost.

## Action needed (Braden)

Review the full flag list in `crm7/src/lib/documents/documentCategories.ts` on branch `fix/document-pipeline-tenant-encryption` (commit `e6dcae36`):
1. Confirm/correct the 15 categories marked `sensitive: true` above.
2. Rule on the `photography_consent` borderline call (currently non-sensitive).
3. Confirm the 7 categories marked `expires: true`.
4. Rule on whether encrypting large WHS photo/video evidence (`photo_evidence`) is acceptable given the base64 round-trip cost.

## Mandatory before merge (FF-SELF-VALIDATION-20260507)

- **Validation loop**: §9.1 output-equivalence — target: Braden's corrected flag list matches what ships, verified via the existing `documentCategories.test.ts` guard (80 entries, no duplicates, 14-group order, legacy-sensitive set intact)
- **Equivalence target**: Braden's sign-off comment on this issue, cross-checked line-by-line against the shipped `documentCategories.ts`
- **Cross red-team**: claude-code diffs Braden's corrections against the branch and confirms the test suite still passes (44/44 per WS-A §7) after any flag changes
- **Skills to load**: `compliance`, GTO/legal domain review, `qa-and-verification`
- **Self-report on divergence**: yes — already recorded above; the flag set should not be treated as final until Braden's review lands here

---
Filed by the issue-filer agent from the 2026-07-14 onboarding-360 pilot. Source: `ws-a-report.md` §5 (F13 fix).

