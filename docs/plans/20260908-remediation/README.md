# BSuite remediation execution pack

## Current execution source map — 22 September 2026

The earlier phase instructions below are historical. In particular, **“Claude is unavailable” is superseded** by the user-started Claude #3208 kickoff; it is not a current dispatch restriction. Historical row counts are provenance, not a live backlog or completion count.

Start with the 22 September Claude estate kickoff and located source map (local operator artifact `/home/braden/.codex/visualizations/2026/09/22/01a0c669-af10-7500-8311-93f794ab9529/docs/plans/20260922-claude-estate-kickoff.md`), then verify the exact assignment in its linked canonical queue. The kickoff authorises only the reserved FutureBuild forms #3208 scope after ownership reconciliation. A kickoff is not proof of worker acceptance or execution. Codex maker task `01a0c25e-3f27-7473-ae8f-a10b2bdf0823` retains sole queue and release coordination; do not create a competing writer or edit the shared queue from that lane. The linked kickoff is a local operator artifact; readers on another host must obtain it from the maker rather than fall back to the superseded routing below.

The 22 September maker routing handoff reports HTTP 200/OK probes for GLM 5.3 Flash, Kimi K3 and Qwen 3.5:397b through Ollama Cloud, 53 proxy tests, and both installed 2.1.278 configuration-schema checks passing. Native Sonnet remains the Claude subscription path; only Nex 2.5 uses OpenRouter. These are dated provider/configuration receipts, not product acceptance or a guarantee of future quota. Follow the kickoff's exact model IDs and verify the executing runtime's provider/model receipt before dispatch. No automatic paid or frontier fallback is authorised.

The existing release contract and standing security/UX requirements remain applicable. BSU owns shared organisation administration under [bsuite#3306](https://github.com/Arcane-Fly/bsuite/issues/3306); CRM7's own-mailbox workflow is application-specific. The #3208 lane does not own BSU manuals or database changes: send factual handoffs to their existing owners and preserve the current release holds. The original evidence and earlier phase text are retained unchanged below.

**Dated note — 23 September 2026.** The 22 September routing receipts above are dated evidence, not current routing.
- `qwen3.5:397b` is retiring and should not be selected.
- The current offload routing state is recorded in the local operator artifact `/home/braden/.codex/visualizations/2026/09/22/01a0c669-af10-7500-8311-93f794ab9529/docs/plans/20260923-grok-ollama-offload.md`. On another host, obtain it from the maker.
- The Claude #3208 session was observed running Opus rather than the selected Sonnet on 22 September. This was reported to the maker, and nothing here authorises a model change.
- The #3208 lane edits this file, the [customization capability register](../../20260908-customization-capability-register-v1.00W.md) and the [estate remediation plan](../20260908-estate-remediation-plan-v1.00W.md) under its reservation's `owned_document_paths`.
- The kickoff link above is written as a code span, not a markdown link, because the docs link check refuses absolute local paths. This is the only deviation from the reviewed patch.

## Historical phase instructions retained for provenance

Start with the [14 September Codex IDE kickoff](../20260914-codex-ide-closeout-refined-v1.00W.md), then the [mandatory release contract](20260908-remediation-release-contract-v1.00W.md). The kickoff requires an independent DoD audit of the preceding reconciliation before any claim that cleanup or convergence is complete.

The current backlog contains **370 issue rows and 370 matching prompts**. The historical source packs recorded 333 then 338; the later canonical queue held 340, and 30 additional rows plus nine related-issue links reconciled the missed live issues. None of these numbers is a completion count. The approximate historical 380 remains unverified. The 40 operator-feedback groups and 661 feature-index rows are separate denominators.

Codex in the IDE coordinates this phase. Claude is unavailable. Do not dispatch external Codex CLI workers. Use deterministic scripts for mechanical tasks, low-tier models for bounded classification, standard-tier models for scoped implementation, and high/frontier only for justified hard planning or independent review. Verify actual Grok/Gemini/Qwen model IDs and access before dispatch; never silently fall back to paid API usage. Cap two active workers by default with one writer per worktree. Read the current [IDE kickoff](../20260914-codex-ide-closeout-refined-v1.00W.md) and the parent remediation-execution rule before acting.

Use [backlog.json](backlog.json) for row-to-prompt mapping; refresh each live issue and its acceptance evidence before dispatch. Shared OAuth 2.1 PKCE, protected production PRs, paused schedules, Monkey Projects, Ollama and Unsloth boundaries remain mandatory. Operator Throughput overflow and Jodie identity findings belong to throughput-486 and throughput-479 and their linked sibling rows.

The original dated evidence remains under [evidence](evidence); it is historical evidence, not current launch authority. [Queue runbook](20260908-remediation-hermes-queue-runbook-v1.00F.md) provides historical mechanics; apply the current ownership and model rules above. Run `python3 verify_prompt_contract.py` to check prompt integrity and row bijection. This verifier cannot certify product completion. The parent `scripts/check-plan-currency-markers.mjs` gate verifies that the active plan marker remains current; it does not certify product completion.
