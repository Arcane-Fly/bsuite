The issue below was found during a code review. Follow the provided context and guidance below and implement a solution

## Issue description
Prevent the trigger editor from falling back to the latest draft before it is known that the workflow has no published version.

## Issue Context
The definition, version list, and published version load independently. The version list is ordered newest-first, so its first row may be a draft even when the definition points to an older published version.

## Fix Focus Areas
- src/pages/workflows/[id].tsx[113-140]
- src/lib/workflows/workflowDefinitionService.ts[234-245]