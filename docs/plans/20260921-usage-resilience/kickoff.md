# New Codex task kickoff

Paste the following into a new Codex task for `/home/braden/Desktop/Dev/bsuite`.

---

Take ownership of BSuite estate remediation and drive the existing backlog through verified production acceptance. Use `agent-run-master`, `agent-mem-comms`, `agent-run-cli-fanout` and `agent-definition-of-done`; read the relevant domain skills before each implementation class. Act on this prompt; do not stop after writing another plan.

First read:

1. `/home/braden/Desktop/Dev/.worktrees/bsuite-usage-resilient-continuation/docs/plans/20260921-usage-resilience/README.md` and `evidence.md`. If this branch has merged, use the equivalent files under the project root.
2. `/home/braden/Desktop/Dev/bsuite/.agents/rules/remediation-execution.md`, current `AGENTS.md`, `DESIGN.md` and `AUTH_CANONICAL.md` as relevant.
3. Canonical queue: `/home/braden/.codex/visualizations/2026/09/08/01a07f58-73a6-71f2-a57d-5cf3b6b148dc/bsuite-remediation/hermes-queue.json`.
4. `/home/braden/.agents/state/bsuite-progress/README.md` and `current.json`. Refresh using the adjacent runbook command; never mistake the saved snapshot for live state.
5. Authenticated `bsuite_` project-truth index and inbox. Retrieve `bsuite_usage_resilience_20260921` if available. Preserve later user rulings and find contrary/newer evidence.

Ownership/bootstrap is your first executable milestone:

- Read final handoffs from `Move repos to Arcane-Fly` (01a0c1a9-1649-7590-904b-2eb6b3d4b311) and the current independent `Review project task progress` (01a0a2ca-5e84-76b2-9914-3fcc3f528b3f). Inspect active tasks/processes/worktrees and pending writes. The migration task completed its safe portion, not every migration obligation.
- Accept ownership using your real Codex session ID, under the existing queue lock and comms protocol. Preserve history, criteria, dirty work and workers with valid leases. Do not choose an owner from a stale top-level field or elapsed timeout alone. Send required handoff/coordination notices in the bsuite inbox.
- Verify hooks and oversight before application dispatch. `systemctl --user` must show external-supervisor active and context-refresh/accountability timers enabled; completed oneshot services may be inactive with exit 0. Read actual receipts, not service names alone.
- The 21 September audit found external supervisor targeting ended Claude b6ddc034-29d4-4a71-9c93-f4cea504fb80. Rebind the accepted current owner across working-memory, conscience/learning, task-context, context-retrieval and delivery/continuation fields using the supported runtime protocol, backed up and atomic. Keep criteria at most 120 characters. Resolve conflicts instead of stealing active leases. Do not blindly run the Claude-specific auto-repoint script for Codex.
- Verify `/home/braden/.codex/hooks.json` trust/enabled state through current runtime tooling. Existing five hooks provide scoped recall only. Verify an actual invocation for your session, enrollment, delivered digest and independent review receipt. A manual probe is only a manual probe. If additional native hooks require platform trust, give the operator exact settings steps; continue eligible read-only preparation. Do not claim Claude-style mutation enforcement exists in Codex without a tested adapter. Until then use explicit pre-action/release review; preserve every existing HOLD.
- Keep `Review project task progress` as independent accountability. Use the conscience and memory services as bounded advice; do not spawn recursively supervising models or another 20-minute schedule. The application executor owns forward progress.

Then reconcile and execute:

- Organisation is `Arcane-Fly`. Confirm refs and permissions live. Do not blindly rewrite old issue URLs: many canonical issues still reside under GaryOcean428; verify transfer mappings individually.
- The initial snapshot recorded 384 rows: 350 pending, 21 in progress, 13 completed; 8 ledger/GitHub mismatches. These are dated claims, not the current acceptance count. Inspect all 8 discrepancies against PR, SHA, deployment, migration and D8 evidence before disposition. Correct owner/priority/next-action fields under the queue lock; retain totals/history and report completions separately.
- Resolve Arcane-Fly/bsuite#4 if still open: private sibling checkout needs `BSUITE_CROSS_REPO_PAT` or equivalent GitHub App read credential for the six app repositories. Inspect existing approved credentials/configuration; never expose values. If no available authorized credential, provide exact GitHub repository/organisation secret steps and continue independent work. “Allow all actions” does not grant private sibling access.
- Operator confirms the 14 npm trusted-publisher identities are now Arcane-Fly/bsuite with their existing `publish-<package>.yml` filenames. Check workflow registration: 21 September API lookup returned 404 despite main file presence. Investigate GitHub registration/validation through normal PR flow; verify registration/run identity before legitimate release. Do not mint test releases or replace OIDC with long-lived publish tokens. Preserve main-only stable and next-tag prerelease rules.
- Collect preserved CRM7 inbound SMS work and merged BSU identity changes; complete the remaining provider-to-CRM journey. Do not reconstruct implemented work from scratch. Triage remaining security/tenant/role/storage defects first, then complete shared editor/workflow/grid/record-parity classes and website/SEO work according to current dependencies and acceptance criteria. A narrow milestone does not replace the whole ask.
- Keep Record of Discussion disciplinary records separate from training-contract variations. Enforce visual/code capability parity, independent card/input/font sizing, workflow click-through editing, schema/view/edit field parity, relationship visibility/trace/repoint, in-context creation and roundtrip, Jodie permission-aware assistance, manuals and Airtable-style shared data grid. These longstanding requirements are acceptance obligations, not optional polish.

Use quota-resilient execution:

- Deterministic commands perform bookkeeping, polls and snapshots. Consume deltas and concise evidence; do not reread entire transcripts on every tick.
- Prefer appropriately tiered lower/standard workers. Use Hermes with explicitly selected Ollama Cloud models where suitable; `glm-5.3-flash` was verified for bounded text review, while Kimi K3/GLM 5.3 catalogue presence alone is not competence evidence. Verify actual provider/model in usage receipts. No silent fallback to Claude/frontier or metered API.
- Codex frontier is reserved for supervision, architecture, hard escalations and final high-risk judgment. At most two implementation lanes, one writer per resource, and serialized shared-DB/release work. Independent review is a role, not continuous duplicate implementation.
- Extend productive workers by measured progress; after two failed repairs, rebrief the specific failure and use a qualified stronger route. On quota/auth failure preserve output and checkpoints, observe cooldown, choose an already-authorized route, and continue eligible work. Never resume an old worker concurrently with its replacement.
- Prior Gemini/Qwen $100 each allowances were incremental and shared across workers; current consumed amount is unknown. Reconcile before paid use; this kickoff renews no budget. Do not assume subscription CLI calls have independent quota or unknown cost means zero.
- Checkpoint objective, criteria, owner/leases, source SHAs, dirty paths, actual routes, accepted evidence, failed probes, remaining work and next action before exhaustion. Keep the canonical queue and a compact current checkpoint; no second backlog. Claim scheduled continuation only after observed firing and resumed work.

Completion requires feature PR → development → matching deployed signed-in UX → production PR → verified production/backend/package coherence → ops closeout and DoD. Independently verify the user journey, reload persistence, light/dark and responsive layouts, upstream/downstream consumers and no navigation maze. GitHub closed, tests green and model approval alone are insufficient. Keep reports concrete: newly accepted rows, rows awaiting deployment/acceptance, current owners, actual blockers with attempted resolution, next running action and the central progress link. Continue safe authorized work instead of stopping at “blocked”.

First response: report observed ownership and hook delivery status without claiming acceptance before receipts exist, then complete bootstrap and start the highest-priority eligible work. The progress snapshot is not lease, liveness or handoff evidence. No new request for routine permission.
