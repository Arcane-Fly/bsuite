---
kind: record
authority: none
owner: bsuite
evidence:
  - docs/audits/20260903-week-in-review-estate-audit-and-remediation-v1.00W.md
  - docs/plans/loop-contracts/20260903-corrective-run-phase1-loop-contract-v1.00W.md
---

# Corrective run: section 7 status as measured 2026-09-04 00:10 AWST

Measured by a read-only lane under the PI (claude-code-bsuite-pi) from `origin/development`, each app's recorded gitlink, merged PRs and the required-status-check lists. Phase 1 rows are copied from the Phase 1 loop contract, not re-derived. A row is done only with production-side or required-gate evidence; a merge alone is partial. Rows marked "not measured" were not checked in this pass and must not be read as clean.

| Id | Ask (one line) | Status | Evidence | What remains | Owner | Size |
|---|---|---|---|---|---|---|
| A1 | Done-contract file read by the stop hook, refuses an open item | done | `~/.agents/hooks/loop-gate.py` wired as Stop hook (`~/.claude/settings.json:71`); 17/17 planted-case fixtures pass (test-hook-fixtures.sh) | nothing | PI | S |
| A2 | Every turn ends with next action in flight + 4-part layout | open | four-part layout used from 12:00 3 Sept; no fixture, rides on A1's hook | a fixture proving the layout is enforced, not just followed | PI | S |
| A3 | `loop armed:` line printed every turn, Monitor+wakeup+cron | done | line printed every PI turn; hook half is loop-gate's must-block-not-in-payload fixture, passing | nothing | PI | S |
| A4 | Per-cycle named-skill checklist before a cycle is accepted | open | advisory only (transcript-sourced); no fixture as of 22:48 AWST 3 Sept | a blocking fixture, not just a transcript check | hub lane | M |
| A5 | Intake: every notes export registered and judged | partial | 160/160 register rows judged with `registered_at`; F-verdict banked at 0; notes-cycle `--strict` green at 360/400 untriaged bank (shrinking) | untriaged bank to reach 0; rides #2970 | main lane | S |
| B1 | D9 "used once" + D10 "nothing left behind" DoD limbs | open | tracked as DOD1/DOD2 in phase1: precedents `nothing_finishes_or_merges_without_an_enforcer_run_dod_approve` and `dod_index_row_identity_and_the_two_stage_d9` recorded; retro enforcer runs on today's merges in progress; crm7#2374 merged 21:18 with **no** enforcer verdict | D9/D10 not yet a blocking gate — still process discipline, not machine-checked | completion-enforcer / PI | M |
| B2 | Bump 5 consuming apps to page-builder 2.6.0 | partial | crm7 `package.json` confirmed at `"@bsuite/page-builder": "^2.6.0"` and `"@bsuite/nav-core": "^1.3.0"` (crm7#2368 merged 09:01, promoted via #2374 13:18) | confirm BSU, conduit, braden, throughput each on 2.6.0 (not checked this pass) | bsuite-platform | S |
| B3 | Turn workflow engine on for real tenant, staged, with run evidence | partial | BSU: workflow execution bridge #1085, `workflow_definitions` schema #1083, `published_at` column #1086, all promoted to BSU main via #1092 "promote: workflow canvas — schema, package, execution bridge, AI gate" (13:31, 1 Sept) | a production `workflow_runs` row on the demo tenant proving step 3 of the 5-step sequence; action steps still meant to be disabled first | bsuite-lead / bsuite-user-advocate | L |
| B4 | Anti-rebuild inventory sweep (enumerate existing docs/packages/half-wired surfaces) before any build, finish-or-supersede in the same PR | open | no script/hook found: `find`/`grep` across `scripts/`, `.github/`, `~/.agents/hooks`, `~/.agents/scripts` for "anti-rebuild"/"inventory sweep" returned nothing | build the check itself; this row is pure process discipline today | every implementer | S |
| B5 | Retire `site_settings` path in braden; dual-read then delete | open | braden repo still references `site_settings`/theme storage in `src/hooks/theme/useThemeStorage.ts`, `src/context/theme/ThemeContext.tsx`, `src/context/theme/themeSettingsStore.ts`, `src/integrations/supabase/legacyTables.ts`, `src/integrations/supabase/database.types.ts`; `tenant_branding` appears in only 2 files | column mapping to `tenant_branding` (34 cols), dual-read, then delete; zero-row table drops for the other 22 relations | bsuite-platform | M |
| B6 | Prove nav overlay with one real tenant's rows | partial | infra exists: BSU `src/pages/Developer/Nav.tsx`, 3 `tenant_navigation` migrations (incl. 20260819 throughput-scope fix) | one tenant's rows inserted through `/developer/nav` and confirmed rendering in all 5 apps (audit says table still 0 rows; not independently re-checked, no DB read this pass) | bsuite-user-advocate | S |
| C1 | Doc link + supersession gates wired | done | PR #2968 merged c18efaaf; advisory workflow `doc-links-and-supersession.yml` present; tool-written baselines | nothing (Phase 1 copy) | pi | — |
| C2 | Shrinking ratchets (classification, grid, dod) | done | PR #2970 merged after 7-issue fix-forward | nothing (Phase 1 copy) | pi | — |
| C3 | Dead-man's switch on every scheduled control | done | merged (crm7#2363, bsuite#2985) and, at the 22:48 measurement, **not yet live** (`cron_watchdog_checkins`/`pg_cron_watchdog_check()` absent from production; routed via bsuite#3021→promotion→applier); LIVE since 00:47 AWST 2026-09-04: applier run 33780540784 recorded crm7 20261115/16/17; cron_watchdog_checkins, pg_cron_watchdog_check(), cron_job_manifest and the cron-watchdog-every-15m job exist on production (read-only effect) | none: the first check-in (00:52) and the observer reading it (run 33781273309, issue #3015 at 00:55) both happened; the two jobs it names (document-retention-sweep-daily, sync-award-rates-weekly) are C3 OUTPUT and need their own rows | db-worker | S |
| C4 | DoD hook blocks a UI claim until D8 ran | done | `~/.agents/hooks/dod-gate.py` wired as Stop hook; 17/17 fixtures pass | nothing (Phase 1 copy) | pi | — |
| C5 | Gate self-tests + required-check inventory | done | bsuite#2988 merged 3f11ddf0; meta-gate `check-required-contexts-producible.mjs` confirmed present in tree; `grant-lint`/C10 now required (both confirmed in this pass's required-contexts list) | nothing (Phase 1 copy) | pi worker | — |
| C6 | Hub self-check validates agent name/description/pairing | open | `test-skill-hub.sh` 38/38 pass; planted-case proof still owed by hub lane | planted empty-name fixture | hub lane | S |
| C7 | Close anon-executable SECURITY DEFINER + RLS-no-policy findings | done | braden#581/#583, bsuite#2984 merged; production effect confirmed (`has_function_privilege(anon,...)=false`) | nothing (Phase 1 copy) | db-worker | — |
| C8 | Restore `onRowClick` on the converted crm7 grids (22 DataGrid files: 10 wired, 7 documented exceptions, ceiling now 0 — the audit wrote 16) | done | measured at crm7 `9308fc1`: 17 of 22 DataGrid files still missing `onRowClick`; C2 ceiling holds at 17; no crm7 PR yet; 02:50 AWST 2026-09-04: crm7#2381 merged (10 wired + 7 exceptions), promotion #2382 pending #2384, pointer + #3027 next; 03:21 AWST: crm7#2382 merged (0bf5e8a9) and crm.crm7.app serves version.json commit 0bf5e8a - the ten wired grids' click-through is in production; the 7 exceptions and the C2 ceiling stand; live artefact deferred to first data (FOLLOW 34) and scroll on return is the estate row (FOLLOW 35) | arm the ratchet at 10 wired; FOLLOW 34/35 as their own rows | main lane | S |
| C9 | Reports Name cell one line, on production | open | D-156 fix rode the 13:25 override promotion (crm7 main `d549912`); column-width persistence still open | per-user column width persisted via `@bsuite/data-grid` | main lane | S |
| C10 | CSS-class gate: unemitted utility fails | done | PR #2975 merged b04c952c; banks BSU 381 / throughput 249 / crm7 10 / braden,conduit,R80.4 0; F8 (BSU#1106) merged to BSU development | context still path-filtered until C5 required-list append completes | gate pi | — |
| C11 | `DESIGN.md` at parent root as judgment layer | done | bsuite#2983 merged; 291 lines, 22 gate scripts named, hub asked to repoint `bsuite-brand-system` | confirm hub repoint landed | design-sheriff worker | — |
| D1 | `operator-notes-verdicts.json` becomes the machine record; every NOT-DONE/PARTIAL row gets a GitHub issue; verdicts written for D-104 to D-139 | partial | file confirmed: 174 rows, every row carries `registered_at`+`verdict` (63 NOT-DONE, 62 PARTIAL, 31 DONE, 13 OPEN, 4 UNVERIFIABLE); `scripts/estate-align.mjs:50` reads this exact path; D-104..139 verdicted 36/36 (0 missing); `gh issue list --search "D-\* in:title"` returns real issues (e.g. D-131, D-139, D-78, D-96, D-74) | confirm all 125 NOT-DONE+PARTIAL rows individually have a GitHub issue (only spot-checked, not exhaustive) | bsuite-plans-keeper | M |
| D2 | One D-numbering scheme: the 3 colliding docs relabel to PR-nn/NB-nn with an old-to-new table; `estate-align.mjs` refuses a new D-nn outside the register | open | the 3 named docs (`docs/20260813-operator-directive-notes-backlog-remediation-v1.00D.md`, `docs/20260814-portals-operator-rulings-v1.00A.md`, `docs/20260825-remeasured-clearance-brief-v1.00A.md`) still live at their original filenames with "D-series continues from D-58/D-92" headers, no PR-nn/NB-nn relabel or mapping table; `grep -n "refuse\|outside.*register"` in `estate-align.mjs` found no such refusal logic | relabel the 3 docs; add the register-outside-refusal check | bsuite-plans-keeper | S |
| D3 | Repair the 60 dangling nav links before archiving; generate `docs/README.md`'s authority block so it cannot go stale by hand | open | `docs/README.md`'s only "dangling-reference repair" block is dated 2026-08-17/18 (a prior, unrelated cleanup) — predates this audit's 3 Sept 60-link finding; no evidence of a fresh repair pass or an auto-generated (vs hand-written) authority block | repair the 60 links named in the audit; make the block generated, not prose | bsuite-plans-keeper | M |
| D4 | Plan-must-contain-a-plan / record-must-have-zero-unchecked | partial | `scripts/check-doc-classification.mjs` and `.github/workflows/doc-classification.yml` both exist in tree | confirm it's required, and the 12 unearned F-docs are resolved (phase1 FOLLOW #21 flags the pathspec on this gate never matches root-level `docs/*.md`) | bsuite-platform | S |
| D5 | Repoint BSU feature-builder Phase 3 at `@bsuite/workflow-canvas`; retire duplicate canvas | open | crm7 now depends on `@bsuite/workflow-canvas": "^0.2.2"` and BSU's workflow execution bridge (B3) promoted 1 Sept, but BSU's `RelationshipCanvas.tsx` still exists as a separate component (not confirmed superseded/banner) | banner the 3-panel spec as superseded; confirm `RelationshipCanvas` given persistence or replaced | bsuite-user-advocate | M |
| D6 | Seed `RoleCapabilityDefaults` only into zero-grant tenants + reset control | partial | `RoleCapabilityDefaults.ts` exists in BSU; BSU#911 "permissions arrive with defaults ticked... (D-51/D-97/D-103 — **third raise**)" merged 27 Aug — the "third raise" phrasing means this has recurred after being merged twice before | confirm this raise held (no 4th recurrence); reset-to-defaults control and precedent record not independently confirmed | bsuite-auth-guardian | S |
| D7 | Every notification/compliance row opens the record it's about (`ComplianceRow` gets an href, `action_url` resolved with backfill) | open | `business-suite-unified/src/pages/GTO.tsx` still defines `function ComplianceRow({ item })` as a plain component — matches `operator-notes-verdicts.json`'s own NOT-DONE evidence ("plain div, decorative ChevronRight aria-hidden, no onClick/Link/href; 12 items carry no target field"); `crm7`'s `action_url` field exists on 3 writer types (`timesheetWorkflow.ts`, `leaveRequestService.ts`, `raiseEmailNotice.ts`) but no reader resolving it against a source-app origin found | wire `ComplianceItem.href`, the action_url reader/backfill; count 6 writers/2 readers as the row asks | bsuite-platform | M |
| E1 | One permission vocabulary, crm7→BSU capability ids, 4-PR sequence | open | `crm7/src/lib/permissions.ts` still present in the tree; no map/shim/seed/delete evidence found | PR 1 of 4 (the reviewed map) not evidenced | bsuite-auth-guardian | L |
| E2 | Adopt `@bsuite/*` shared packages, delete local copies | partial | crm7 `package.json` already depends on `@bsuite/ui ^1.3.0`, `@bsuite/auth 1.0.0`, `@bsuite/nav-core`, `@bsuite/page-builder`, `@bsuite/workflow-canvas`, `@bsuite/dates` — but 65 files remain under `src/components/ui/` and 13 under `src/components/magicui/` undeleted | delete-the-copies half of each cluster (183 paths per audit) | bsuite-design-sheriff | L |
| E3 | One feature-flag system (`tenant_features`, drop `tenants.feature_flags`); one email-send path (`email-dispatcher`, 4 callers migrated) | partial | `tenant_features` table + BSU `Developer/Features.tsx` toggle (upserts on tenant_id/feature_type/feature_key) both exist; `email-dispatcher` (BSU edge function) is already the shared path called from crm7 (`ai/tools/edge-call.ts`, `ai/tools/calendar-lookup.ts`), conduit (`communicationService.ts`, `sms/index.ts`, `talentCommunity/optInEmail.ts`) — broadly adopted already | confirm `tenants.feature_flags` JSON readers are gone (not checked this pass); confirm 0 remaining non-dispatcher send paths | bsuite-platform | M |
| E4 | Migration ownership: BSU-only, delete parent duplicates; nightly `list_migrations` reconcile of the 45 orphan versions | open | root `supabase/migrations/` still exists as its own scope alongside BSU's (`git ls-tree` confirms the path is still tracked; `supabase-migrate.yml` still triggers on both `supabase/migrations/**` and `business-suite-unified/supabase/migrations/**` as separate paths); no nightly reconcile workflow found (`ls .github/workflows` has no reconcile/nightly-migration match) | delete the parent duplicates; build the nightly `list_migrations` diff job | bsuite-platform | S |
| F1 | Tenant rung for layouts (`tenant_layout_defaults`) | open | `gh search code "tenant_layout_defaults"` returns only the audit doc itself — table/hook not built | migration + `usePageGridLayout` resolver + tests | bsuite-platform | M |
| F2 | Element-scope styling (per-element, tenant/personal/platform) | open | depends on F1; page-builder 2.6.0 now reaches crm7 (B2 partial) but no `elementScope`/`ElementStyle` code found | the per-element store and resolver | bsuite-platform | M |
| F3 | Authoring surfaces: email/comm templates, report-param editor, per-tenant SMS/Stripe/Resend creds, CSV/XLSX import-export on `/admin/data` | partial | `crm7/src/pages/admin/data.tsx` exists (the target surface); `report_templates.default_parameters` is read into the UI (`reports/[key].tsx`) though no writer/editor confirmed; `/settings/integrations` pattern already exists and is live (Xero OAuth callback wired); `csv.ts`/`renderF17Xlsx.ts` exist for export | no dedicated email/comm-template editor found (only `EmailTemplateSelector.tsx`, a selector, not an editor); CSV/XLSX **import** on `/admin/data` and SMS/Stripe/Resend creds under `/settings/integrations` not confirmed | bsuite-platform | L |
| F4 | Enterprise-admin scoped equivalents under `/admin`: nav, feature toggles, branding, permissions, custom pages, templates, RLS-gated | partial | BSU `/admin` already substantial: `AdminBranding.tsx`, `PermissionsEditor.tsx`, `PlatformKit*.tsx` (Auth/Logs/Secrets/Storage), `OAuthClients.tsx`, `LicenseManager.tsx`, `AuditLog.tsx` all exist | no `/admin` equivalent found for nav entries, feature toggles, custom pages or templates — those still live only under `/developer` (per B6/E3's own evidence) | bsuite-lead / bsuite-platform | L |
| F5 | Send-email action on every external-party record + composer reading URL params | open | `EmailComposeDialog.tsx` exists at `crm7/src/components/email/EmailComposeDialog.tsx` — matches the audit's own "0 consumers, anti-rebuild candidate" description; no evidence it now reads `to/subject/body/returnTo` or is wired to the 7 record types | wire the composer + 11 detail pages | bsuite-platform | M |
| F6 | Finish branding hierarchy; per-app logo variants; prove login page both themes | partial | BSU `useBranding.ts` + 2 `branding_json_for_tenant` migrations exist (deployed per audit's own note "since 1 Sept") | live visual proof on login + 5 sibling surfaces (out of scope for a read-only pass — needs Playwright) | bsuite-design-sheriff | M |
| F7 | `/settings/data` + `/admin/data` Airtable-style grids, editable, scoped | open | `BrowseDataTab.tsx` exists at `crm7/src/components/admin/BrowseDataTab.tsx` (same dead/underused pattern as F5) | remove `slice(0,4)`, add per-user column picker, join contract for linked columns; close crm7#1477 | bsuite-platform | L |
| F8 | Restore dropped card/button colours (BSU `b391c82`, 7 May regression) | partial | ties directly to C10 (done): "F8 (BSU#1106) merged to BSU development" per C10's own evidence | confirm BSU#1106 promoted to BSU main and both-theme render proven | bsuite-design-sheriff | S |
| G1 | Close FutureBuild email chain: MS+Google connect/send, verified publisher, scope verification | partial | heavy Sept-3 activity: crm7 #2354 (3-pane mail client), #2356 (compose in reading pane), #2364 (inbound sync upsert-key fix); BSU #1105/#1109/#1111 (mailbox 403, confirm-ownership, pending-mailbox explain) — #1109 and crm7#2374 both promoted to main same day | verified-publisher MCPP path, Gmail scope verification (vs "unsafe app" bypass), mailbox-to-initiating-user attach — none confirmed this pass | bsuite-lead / bsuite-auth-guardian | L |
| G2 | Prove money paths on real providers: Stripe, Xero, SMS phases | partial | BSU SMS provisioning promoted to main (#984/#987/#989/#990); Stripe webhook fix merged BSU#1050 "make the billing webhook actually receive and record payments"; crm7 Xero sync #1327 (31 Jul, predates audit); crm7#2365 "a number comes off the record" (SMS) | a real Stripe test-payment row and a real Xero round-trip, both with recorded evidence — not verifiable read-only | bsuite-platform / completion-enforcer | M |
| G3 | Close console gaps: connect a real tool per console, else a one-shot hand-runbook (operator-facing) | open | `docs/runbooks/` holds 6 guides, all dated mid-Jul/mid-Aug (16 Jul x4, 13 Aug), none post-dating the 3 Sept audit's specific console list (Azure, Google Cloud, Mobile Message, npm, Vercel env) | **operator action**: several of these consoles have no API at all — the remedy is a hand-runbook per console, which is inherently operator-paced, not fully automatable | agy-worker / hermes-worker | S |
| G4 | Training chain: wire `training_plan_id` / unit linkage | open | `gh search code "training_plan_id"` found no hit in any GaryOcean428 repo | identify writer, wire TGA import + unit linkage, prove on demo tenant | bsuite-platform | M |
| G5 | Website/embed leads: notify owner + confirm lead + log + route by rule | open | `tenant_settings.lead_notification_email` column already exists (BSU migration `20260422140100`, predates audit by months) but no PR evidence found of a UI control writing it, or of the notify/confirm/log flow | UI control + evidence pack (1 real owner email, 1 confirmation) | bsuite-platform | M |
| H1 | Gitlink-lag gate in parent (push-time compare, auto-bump PR) | done | tracked as **GITLINK** in phase1 (PR #2969: `check-gitlink-in-app-main.mjs` + `advance-submodule-pointers.mjs`); required context **"Every gitlink sits on its app's own main"** confirmed present in `main`'s required_status_checks this pass | nothing | bsuite-platform | — |
| H2 | Work-at-risk sweep: 24h-no-PR branch → issue; 0-ahead merged branches deleted same-turn | partial | no matching script/workflow in `bsuite-pi` or `~/.agents`; the one similarly-named hub artefact (`~/.agents/hooks/pre-destructive-git.py`, commit `4a5eaa9`, 3 Sept) is a **different** control — it snapshots+logs a destructive git command (force-push/reset --hard) rather than sweeping stale branches; 03:17 AWST: bsuite#3039 built (scripts/work-at-risk-sweep.mjs (planned) + .github/workflows/work-at-risk-sweep.yml (planned); dry run over 7 repos: 16 branches, 0 at risk, 3 deletable); first enforcer SEND_BACK (stale-merged classified by branch NAME; per-branch fetch aborted the run) fixed at 02fbbf76 (mergedAtTip; ERROR rows; 15/15 self-tests); third-round merge-ready (protected prefixes archive/ attic/ presnap/ release/ prod*; 20/20; dry run 19/0/2) and MERGED e71a8ac7 at 04:12 AWST; the schedule and dispatch only exist from the default branch, so the first run follows promotion 4 | D9 closes on the first scheduled run's issue after promotion 4 | bsuite-plans-keeper | S |
| H3 | One mutation lane per worktree via `bsuite_lease_<repo>` claim before checkout; contested claims go to the PI | partial | fully documented and used: `~/.agents/skills/agent-mem-comms/references/lane-protocol-20260903.md` "The lease" section gives the exact claim JSON, and records one measured live CONTESTED instance (a second session's takeover on 3 Sept, resolved by the PI, "patch first, then rule, then commit") | not machine-enforced — no gate blocks a checkout that skips the claim | every lane | S |
| H4 | Rewrite the skill/MCP pairing record; hub sweeps (mirrors, lock re-key, provenance, rename routing, description budget, CLAUDE.md, silo guard) | partial | hub commits confirm most sub-items done: `858546e` one pairing record resolved to live names + hub self-check; `ffd8f6a` re-keys the 16 stale `.skill-lock.json` entries + restores `biz-poloniex-futures` provenance; `278c002` adds a routing-records rename phase; `23d2616` description-budget pass (73,510→58,772 chars); `e73f96f` adds the `polytrade_` prefix to silo-guard (confirmed live, `~/.claude/CLAUDE.md:114`); `~/.claude/CLAUDE.md`'s app line now correctly says "6 apps" | confirm the ~70 agent-mirror sweep count independently (commit `9c6118b` addresses it but the count wasn't reverified) | accountability-agent | M |
| H5 | Fan-out plans declare a 429 fallback route up front; re-dispatch on it without asking | done | `~/.agents/skills/agent-run-cli-fanout/SKILL.md` item 8 states the exact chain (`:free` id → paid id → Claude Sonnet, moved on the same turn on a 429) and explicitly names it as closing "RC-H H5", citing the audit's own three re-runs as the template (commit `cc36dcd`) | nothing | PI lanes | S |
| H6 | Finish trusted publishing: delete 16 `NPM_TOKEN` refs, keep `id-token: write` | partial | measured this pass: `NPM_TOKEN` still referenced in 16 `.github/workflows/publish-*.yml` files; `id-token: write` present in 17 workflows — both coexist, the deletion has not happened; MERGED bsuite#3026 (1c79a202) and promoted to main 293479db (#3029): the 16 references were stale comments, removed; id-token kept; OIDC proven by npm _npmUser trusted-publisher marker on nav-core/theme/data-grid/ui | D9 closer: the next real @bsuite publish succeeding via OIDC (run id) | bsuite-platform | S |
| H7 | Sweep temp/scratch files estate-wide, add count to weekly measure | partial | `scripts/reclaim-ephemera.mjs` and `scripts/check-worktree-hazards.mjs` both exist in tree, addressing the described ~6.4GB finding | confirm wired into J6's weekly measure | bsuite-plans-keeper | S |
| I1 | Pre-flight (precedent/docs/code search, all logged) before any question reaches the operator | done | `~/.agents/skills/agent-mem-precedent-rule/SKILL.md` §6a "Pre-flight before ANY question leaves for the operator (mandatory, logged)" + `references/escalation-gate.md:61` gives the exact 3-line logged format ("searched: precedent <n> · docs <n> · code <n> — a packet without these three lines is sent back unread") (commit `93c539c`) | nothing | every lane | S |
| I2 | Generalise at (the audit's own) recurrence: a second dated addendum ends with a class statement + the enforcing gate/hook | done | `~/.agents/skills/agent-mem-precedent-clerk/SKILL.md:68-69` states verbatim: "Generalise at first recurrence. When a memory file or record receives its second dated addendum, the addendum must end with a class statement — 'the rule is not X, it is Y'" (commit `5636018`) | nothing | agent-mem-precedent-clerk | S |
| I3 | Schedule precedent-index reconciliation; dedup-before-write | partial | `~/.agents/scripts/precedent-index-reconcile.py` and `.sh` both exist | confirm scheduled (cron/workflow), not just present as a script | agent-mem-precedent-clerk | S |
| I4 | Cross-reference TODO.md / MCP sleep packets / `.remember/`; session-end reconcile step | partial | `.remember/` exists at 3 levels (`~/.remember`, `~/.claude/.remember`, `~/.agents/.remember`) | no `TODO.md` file found in this checkout to confirm the cross-pointer or the session-end reconciliation step itself | bsuite-plans-keeper | S |
| I5 | qig-memory write-boundary schema validation (reject non-string content) | not measured | the row's own audit text says "out of this repo's scope" — the qig-memory server is a separate repo not reachable read-only from `bsuite-pi` | belongs to the qig-memory server owner, not this pass | qig-memory server owner | S |
| J1 | Secret scanning: extend gitleaks to full-history + 6 app repos; price paid tier | partial | parent `secret-scan.yml` runs gitleaks on every push (`branches: ['**']`) and PR; `gitleaks` confirmed **required** on both `main` and `development` in this pass's required-contexts list | confirm the same required check exists on the 6 app repos (not checked this pass); the paid-tier decision is the operator's | bsuite-platform / **operator** | S |
| J2 | Publish-triggered consumer fan-out (one bump PR per publish) | done | `.github/workflows/consumer-lockfile-reach.yml` confirmed: triggers on `workflow_run` of every publish workflow + nightly backstop, opens one PR per reachable app via `scripts/regen-consumer-lockfile.mjs`; live effect seen this pass (crm7#2368 exactly this pattern) | nothing | bsuite-platform | — |
| J3 | `Closes #N` becomes checked, not documented | done | `.github/workflows/development-merge-issue-closer.yml` confirmed: fires on merge-to-development + hourly 7-repo sweep, explains why (all repos default to `main`, PRs target `development`) | nothing | bsuite-plans-keeper | — |
| J4 | Judge every register row the day it lands; 7-day ratchet | partial | `scripts/set-dod-status.mjs`, `scripts/operator-verdict-baseline.json`, `scripts/bsuite-notes-cycle.mjs` all exist; A5 confirms 160/160 rows judged with `registered_at` | confirm the 7-day-without-verdict ratchet is wired as a failing check | bsuite-plans-keeper | S |
| J5 | Open or delete `crm7/chore/lockfile-reach`; stop branch-name reuse | done | no such branch exists on crm7 now; superseded by crm7#2368 (merged, "let nav-core 1.3.0 and page-builder 2.6.0 actually reach this app") and #2369 (closed, unsigned-commit attempt) | nothing | bsuite-platform | — |
| J6 | Weekly "asks landed" PR-title classifier ratio (feat/fix vs mechanics) in the sleep packet | open | no script/workflow matching "pr-title-classifier"/"asks_landed"/"asks landed" found in `scripts/` or `.github/workflows/` | build the classifier | bsuite-plans-keeper | S |
| J7 | Fix migration-history audit checker (comments, allowlist), rename nothing | done | PR #2971 merged 364fd281; Class C green on every PR (Phase 1 copy) | nothing | pi | — |
| J8 | Reconcile rulesets vs classic branch protection across 7 repos; weaker list matches stronger; `enforce_admins` on for crm7/R80.4 | open | no script reads both together; the closest existing gate (`scripts/check-required-contexts-producible.mjs`) explicitly disclaims it ("anything about rulesets. Ruleset-level required checks are configured [elsewhere]"); no `enforce_admins` toggle script found | build the reconcile script; turn on `enforce_admins` for the 2 named repos | bsuite-platform | S |
| J9 | Port crm7's test-typecheck ratchet to BSU + R80.4 | open | `check-test-typecheck-ratchet.mjs` (crm7's own template, per audit) not found anywhere in the parent scripts tree, nor a BSU/R80.4-side equivalent located | port the script; measure R80.4's count first (audit notes it's unknown, prior measurement run failed) | bsuite-platform | S |
| J10 | Un-stick 4 refused gitlinks (conduit, R80.4, braden, throughput) by descendancy | done | tracked as **GITLINK** in phase1: conduit/R80.4/throughput read current; crm7 was the one refused (development tip) — and crm7 has since promoted to main (crm7#2374, 13:18 3 Sept, confirmed this pass via merged-PR search) | confirm `check-gitlink-in-app-main.mjs` is now green for all 6 apps post crm7 promotion | pi | — |
| J11 | Seed step + its own applied-seeds ledger on `supabase-migrate.yml`; rule prod-vs-demo on the apprentice-placement seed; trim duplicate CI triggers | partial | `supabase-migrate.yml` has a real ledger-refusal step (`check-migration-version-already-in-ledger.mjs`, general migration ledger, not seed-specific); `scripts/generate-apprentice-placement-workflow-seed.mjs` exists and emits an idempotent seed SQL file, but no production-vs-demo ruling found in it or nearby docs; 39 of ~106 workflow files still fire on both `push:` and `pull_request:` (dedup not done) | a dedicated seed-ledger step; the prod/demo ruling; the trigger dedup pass | bsuite-platform | M |
| J12 | Manage agent guidance as software: every correction routed to exactly one layer (check/mechanic/prose), voice-class counts week over week, re-open a fix whose count doesn't fall, rerun the same fixed eval set | partial | `~/.agents/scripts/evolution-report.py` §11(c) "complaints by the operator's voice class (steer + false_complete)" implements the exact weekly per-voice-class (V1-V17) count this row asks for (commit `36cf0c0`); hub commit messages consistently name the layer they land in (e.g. `5636018` "every lesson routes to exactly one layer") | no confirmation that a stalled-count class actually re-opens its fix, or that the same fixed eval set is rerun after every guidance change | PI / bsuite-plans-keeper | — |

## (a) Rows the audit itself flags as needing the operator

- **J1** — Secret Protection (push protection + full scanning on private repos) is a per-committer paid GitHub add-on; the free half (gitleaks, already required) is done, the paid half is an explicit purchase decision for the operator.
- **G3** (indirectly) — several console gaps have no API at all (Mobile Message, some Azure/Google Cloud consoles) and the audit's own remedy is a one-shot hand-runbook, not automation — implicitly operator-executed.
- **F6 / G1** — branding and OAuth verified-publisher paths (MCPP legal-entity verification, Gmail scope review) route through Google/Microsoft's own human review queues, not something a lane can force.

## (b) Rows that look superseded by later work today (3 Sept, after the audit was written)

- **D6** — BSU#911 already claims to close D-51/D-97/D-103, but the PR title itself says "third raise," meaning two earlier merges did not hold. Treat as a recurring-defect pattern, not settled by the existence of a merge.
- **F8** — folded entirely into **C10**'s evidence chain (BSU#1106 merged as part of the C10 gate PR's fix-forward); F8 should probably stop being tracked as an independent row once C10's promotion is confirmed.
- **J5** — the specific branch named in the audit (`crm7/chore/lockfile-reach`) no longer exists; superseded by crm7#2368/#2369/#2374, which is the general mechanism J2 now automates going forward.
- **N1/N2** (not section-7 rows, but tracked alongside J7/GITLINK in phase1) — these are a same-day operator directive (D-160, 10:48 AWST) layered on top of, and now gating, every app promotion; they postdate and partially override the order of attack in §7.

## (c) Three highest-leverage rows to brief first

1. **G1 (FutureBuild email chain)** — explicitly client-blocking in the audit's own words ("not done until email both microsoft and google work and are proven to work"), already has the most same-day merge velocity of any row (5+ PRs today across crm7 and BSU, two already promoted to main), and is the one row where "partial" is closest to "done" — a focused brief closes real client pain fastest.
2. **H6 (trusted publishing / NPM_TOKEN)** — small, mechanical, fully measured this pass (16 files, exact list derivable from `git grep`), and a security-adjacent gap (a live fallback token sitting next to OIDC in 16 workflows) that a single PR can close in one sitting — high leverage-per-effort.
3. **E1/E2 (permission vocabulary + shared-package adoption)** — both open/partial with concrete, countable remainders (crm7's `permissions.ts` still live; 65+13 duplicate UI files still undeleted despite the packages already being dependencies) — this is the class of work the audit says is silently recurring estate-wide (F-15, F-20), and briefing it now prevents another week of parallel drift.

## Not independently measured

I5 only — the audit's own row text puts it outside this repository (schema validation at the qig-memory write boundary belongs to the qig-memory server owner). The 21 rows left unmeasured at 00:10 were measured at 03:00 AWST from origin/development, the app gitlink trees and the hub's commit history; their evidence is in the table above.
