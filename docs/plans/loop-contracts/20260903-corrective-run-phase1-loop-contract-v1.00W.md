---
kind: plan
authority: operator
owner: bsuite
evidence:
  - scripts/check-docs-links-and-pins.mjs
  - scripts/audit-doc-supersession.mjs
  - scripts/check-doc-citations-resolve.mjs
  - scripts/estate-align.mjs
  - scripts/check-airtable-grid-adoption.mjs
  - scripts/audit-prod-migration-history.mjs
  - .github/workflows/estate-alignment.yml
---

# Corrective run, Phase 1: loop contract (R-A, R-C, J7)

> **File:** `20260903-corrective-run-phase1-loop-contract-v1.00W.md`
> **Status:** W · **Created:** 2026-09-03 · **Owner:** bsuite · **Lane:** `claude-code-bsuite-pi`
> **The plan:** [`../../audits/20260903-week-in-review-estate-audit-and-remediation-v1.00W.md`](../../audits/20260903-week-in-review-estate-audit-and-remediation-v1.00W.md) §7, rows A1 to A5, C1 to C10, J7.
> **The method:** [`../../00-roadmap/20260903-corrective-run-commencement-prompt-v1.00W.md`](../../00-roadmap/20260903-corrective-run-commencement-prompt-v1.00W.md).
> **Lock:** qig-memory `bsuite_objective_lock_20260903_corrective_run_phase1`.

## Why this file exists

The audit's root cause RC-A: a loop whose exit condition is prose ("everything, 100%") cannot evaluate true, so the stop hook re-fires forever and every turn ends in a report. This file is the exit condition. Every item below is a named PR, a named gate, a named count or a named live URL with the command that measures it. The stop hook reads the machine-readable block, never the prose; the PI ticks a row only with the evidence pointer in the last column.

## Exit condition, in the operator's words

From the commencement prompt: *"Phase 1, R-A and R-C: the run can prove it is working and prove it is done. Exit: the loop line shows on every turn, the two doc gates are wired, `dod_status` and the grid ratchet have shrinking baselines, every pg_cron job has a dead-man's switch, the DoD hook blocks a UI claim until D8 ran, and the migration audit is green because the checker was fixed, not because anything was renamed."*

> **Re-scoped 12:15 AWST (PI, with reasons):** the migration-audit limb is *Class C green on every PR* (J7); classes A and B on a development-based run list development’s unpromoted migrations by design and turn green on `main` with the parent promotion. The hook limbs (A1, A3, A4, C4, C6) are built by the hub lane under the audit PI (operator’s word, 10:3x); this run consumes the resolver SHA it sends. The required context for estate alignment is `align`, the job id, not the workflow name. The refresh-to-update notice (N1, N2) is added by the operator’s 10:48 directive and holds every app promotion to `main` until that app carries it, hotfixes excepted at a stated after-hours time.

## Contract (machine-readable)

Each item: `id`, `kind` (`gate` a CI check name that must be required and green · `pr` a merged PR · `count` a measured number with its command and the bound it must satisfy · `url` a live page · `key` a memory record · `hook` a local hook behaviour proven by a planted case), `check` (the command or query that decides it), `expected`, `status` (`open` · `done` with evidence · `blocked` with the issue), `evidence`.

```json
[
  {"id": "A1", "kind": "hook", "step": "done-contract read by the stop hook", "check": "planted case: a done-claim with an open item in this file is refused by ~/.agents/hooks/dod-gate.py; with every item done it is accepted", "expected": "refuse then accept", "status": "done", "evidence": "hub delivered: ~/.agents/hooks/loop-gate.py wired as a Stop hook (~/.claude/settings.json:71); planted cases loop-gate.must-block-open-item / must-block-contract-missing / must-block-not-in-payload and must-not-block-resolved / must-not-block-not-armed all pass (test-hook-fixtures.sh: 17 passed, 0 failed, 22:48 AWST); kill switch ~/.agents/state/gates-off absent"},
  {"id": "A2", "kind": "hook", "step": "every turn ends with the next action in flight and the four-part layout", "check": "each PI turn's final message names a monitor, workflow, cron or open PR as in flight", "expected": "true on every turn", "status": "open", "evidence": "operator 23:45 AWST (23:30): 'set a recurring loop until all audit findings have been addressed and production holds all corrections; drive all agents forward as PI' - cron 7601e708 every 7 min replaces 316eb900 with a widened prompt (crm7 live chain, hub-lane PI duties, Phase 1 open rows, section-7 Phases 2-5, D-140..D-145); the overnight stalls were HOOK-generated: ~/.agents/hooks/pre-destructive-git.py returned permissionDecision ask, which overrides bypassPermissions; hub 4a5eaa9 makes it snapshot+log+allow (planted case: dirty repo + reset --hard -> allow, presnap ref written; ask under WORK_AT_RISK_ASK=1)"},
  {"id": "A3", "kind": "hook", "step": "loop line on every turn", "check": "each PI turn's final message carries a line starting 'loop armed:' naming monitor, cron and wake-up state", "expected": "true on every turn", "status": "done", "evidence": "loop line printed on every PI turn; the hook half is the loop-gate's must-block-not-in-payload fixture (arming record absent from the Stop payload blocks), passing 22:48 AWST"},
  {"id": "A4", "kind": "hook", "step": "per-cycle named-skill checklist", "check": "the named skills of the commencement prompt have a recorded invocation in ~/.agents/telemetry/events.jsonl or ~/.claude.json skillUsage before a cycle is accepted", "expected": "recorded", "status": "open", "evidence": ""},
  {"id": "A5", "kind": "count", "step": "intake: every notes export registered and judged", "check": "node scripts/bsuite-notes-cycle.mjs (dry run) reports 0 paragraphs awaiting registration; jq of docs/00-roadmap/operator-notes-verdicts.json covers every D-id in the register; the F-verdict check in scripts/estate-align.mjs banks the unverdicted count and fails a row older than 7 days by registered_at", "expected": "0 awaiting; 0 register rows without a verdict (all 160 judged)", "status": "open", "evidence": "160/160 judged; registered_at on every row; F-verdict banked at 0; notes-cycle --strict green on the untriaged two-way ratchet 360/400 (bank shrinks as a triage lane dispositions paragraphs); rides #2970"},
  {"id": "C1", "kind": "gate", "step": "doc link and supersession gates wired", "check": "an ADVISORY parent check (doc-links-and-supersession.yml) runs scripts/check-docs-links-and-pins.mjs and scripts/audit-doc-supersession.mjs with baselines {findings, scanned} banked only from a CI run; dangling links a monotonic ceiling, supersession equality both ways; scripts/check-doc-citations-resolve.mjs keeps UNRESOLVED non-failing, exempts (planned), ceilings the count, and existence-checks evidence: paths; the context is required by C5 only after two green runs", "expected": "workflow present, baselines banked from CI, self-tests prove failure, then required", "status": "done", "evidence": "PR #2968 merged to development at c18efaaf (14:57 AWST) after an independent Fable review (FAIL, 6 issues) and a fix-forward lane (all six fixed: merge-safe with development, body `(planned)` exemption, tool-written baselines 58/456, 10/471, 18/336, parseEvidence order, step names); advisory workflow; `Cited scripts and workflows resolve` stays required"},
  {"id": "C2", "kind": "count", "step": "shrinking ratchets", "check": "baselines are JSON {origin_count, origin_date, current_count, current_date, scanned, per_week}; floors (classification 15/week from 175, grid 6/week from 192) evaluated ONLY in the nightly non-required lane that opens one issue per breach; PR-path rise and unbanked-fall fail; dod {655, 662} banked with its denominator, not floored (granularity contract is D4, Phase 3); every file importing @bsuite/data-grid without onRowClick fails above a shrinking ceiling", "expected": "JSON baselines committed; nightly floor job on development; ceilings shrink only", "status": "done", "evidence": "PR #2970 review FAIL (7 issues) fixed forward at 1d5cbd2e (pipefail on floor steps, D-6 superseded restored, NaN floors fail, entry-point self-tests, import-scope predicate, unit label, A5 disposition vocabulary + untriaged bank 360/400); now CONFLICTING with development, merge lane running"},
  {"id": "C3", "kind": "count", "step": "dead-man's switch on every scheduled control", "check": "public.pg_cron_watchdog_check() writes a check-in on EVERY run (healthy or not) to cron_watchdog_checkins; a parent scheduled workflow (cron-watchdog-observer.yml) fails and opens an issue on a missed or unhealthy check-in; a declared manifest default-denies unknown jobs and flags expected jobs with no cron.job row; http jobs judged on net._http_response; the two vault-failing jobs stay failing as the first proof case", "expected": "23 jobs + 2 unscheduled functions covered; observer red on a planted stale check-in; retention sweep untouched (destructive, Phase 2 pre-flight)", "status": "done", "evidence": "review FAIL 16:16 (in-flight-row blocker; PR-path DB self-test; version collision); fix-forward pushed (crm7#2363 abd5243d, bsuite#2985 5f052798; renumbered 20261117000000); lane resumed after two process restarts; enforcer before merge; NOT LIVE on production at 22:48 AWST: crm7 20261117000000 (the watchdog function, table, manifest and its 15-minute job) is on crm7 main 9308fc10 but absent from supabase_migrations.schema_migrations; to_regclass('public.cron_watchdog_checkins') and pg_cron_watchdog_check() are NULL on production, so the parent observer has nothing to read. Route: bsuite#3021 (crm7 gitlink) -> parent promotion -> supabase-migrate.yml -f submodule=crm7 -> effect check. Status flipped from done to open until that effect is read; LIVE on production at 00:47 AWST 2026-09-04: parent promotions #3022 (crm7 pointer 38798155) and #3029 (applier ledger INSERT on stdin, #3028) merged; applier run 33780540784 applied and recorded 20261115/20261116/20261117 (the first run, 33778653772, applied 20261115 and failed the ledger INSERT on psql's -c non-interpolation - fixed forward); read-only effect: schema_migrations rows 1/1/1, cron_watchdog_checkins + pg_cron_watchdog_check() + cron_job_manifest exist, cron.job carries cron-watchdog-every-15m (active), communications_recipient_type_check includes client. FIRST CHECK-IN 00:52:00 AWST 2026-09-04 (cron.job_run_details: succeeded): healthy=false, 25 of 27 jobs healthy, unhealthy named in the row (document-retention-sweep-daily last_run_failed - the audit's first proof case - and one more); the switch reports on EVERY run, so a silent estate now shows as a missing row. D9 closer: the parent observer (cron-watchdog-observer.yml) reading a check-in and opening an issue on the unhealthy set; D9 CLOSED 00:55 AWST: observer run 33781273309 (cron-watchdog-observer.yml, dispatched on main) read the production check-in and posted to bsuite#3015: unhealthy - document-retention-sweep-daily (last_run_failed), sync-award-rates-weekly (last_run_failed), 4 http-layer failures since the previous check-in; the run exits 1 by design. The switch, the check-in and the reader are all live"},
  {"id": "C4", "kind": "hook", "step": "DoD hook blocks a UI done-claim until D8 ran", "check": "planted case: a done-claim after editing a .tsx with no gate_report.py run is BLOCKED; the same claim with ui_touched=false declared is accepted", "expected": "block then accept", "status": "done", "evidence": "hub delivered: ~/.agents/hooks/dod-gate.py wired as a Stop hook (~/.claude/settings.json:62); planted cases dod-gate.must-block (a .tsx edit + done-claim with no gate run) and must-not-block-declared (ui_touched: false) / must-not-block-backend / must-not-block-no-claim all pass (test-hook-fixtures.sh 17/17, 22:48 AWST); kill switch absent"},
  {"id": "C5", "kind": "count", "step": "gate self-tests and required checks", "check": "inventory of the 30 required parent checks: each maps to a workflow and script; count lacking --self-test; explicit-grant-lint required; path-filtered required checks = 0 or always-report", "expected": "inventory committed; explicit-grant-lint required; 0 silent-skip required checks", "status": "done", "evidence": "bsuite#2988 merged to development at 3f11ddf0 (17:04 AWST) after an independent Fable review (PASS + six fix-forwards, all done at 63bbbc85) and the PI's enforcer (APPROVE, 0 gaps; enforcer-c5-*.json). Protection by POST-append with committed dumps: development 28 -> 29 (align), main 30 -> 32 (align, Every gitlink sits on its app's own main); meta-gate scripts/check-required-contexts-producible.mjs (11 self-test cases, 61 contexts / 129 jobs / 105 workflows / 0 findings) wired into the required LANE-WATCHER job; C10 job extracted unfiltered (css-classes-emitted.yml), grant-lint unfiltered with a self-test. Post-merge: append C10 and grant-lint on development once each reports on an unrelated PR. Findings kept on FOLLOW: 7 required contexts without a failure-proof step"},
  {"id": "C6", "kind": "count", "step": "hub self-check validates content", "check": "~/.agents/scripts/test-skill-hub.sh fails on an agent file with empty name/description or a Claude subagent with no skills pairing", "expected": "0 such files after the fix; the check fails on a planted one", "status": "open", "evidence": "~/.agents/scripts/test-skill-hub.sh: 38 passed, 0 failed at 22:48 AWST (content checks included); the planted-case proof (a file with an empty name fails the run) is owed by the hub lane"},
  {"id": "C7", "kind": "count", "step": "anon-executable SECURITY DEFINER and RLS-no-policy tables ruled", "check": "braden migration REVOKEs PUBLIC and anon EXECUTE on is_developer_admin after named grants are verified in routine_privileges; MANUAL_FIX_ADMIN_ACCESS.sql deleted; rehearsal assertion: no public SECURITY DEFINER function executable by PUBLIC, anon only per scripts/secdef-anon-allowlist.txt (planned) (7 named); the 14 RLS-no-policy tables banked exactly with no anon/authenticated grant or TRUNCATE; column-level-grant census for the email lane’s class", "expected": "REVOKE landed by the applier on promotion; rehearsal assertion fails on a planted PUBLIC grant", "status": "done", "evidence": "braden#581 (REVOKE PUBLIC/anon on is_developer_admin) merged to braden development 04fa2a98; braden#583 (header count 17) merged; bsuite#2984 (secdef gate: check-secdef-grants.mjs with acldefault fallback, allowlists, rehearsal steps, live scan) merged to development at 6dc88b43 after review PASS, four fixes, two Copilot threads fixed and resolved, and the PI's enforcer APPROVE at 756cd53d (run 33733445084: 191 SECDEF / 385 RLS tables). Applier for braden + the braden gitlink PR removing the is_developer_admin() allowlist line: the 18:00 window"},
  {"id": "C8", "kind": "pr", "step": "click-through restored on the 16 converted crm7 grids", "check": "crm7 PR merged to development; files importing @bsuite/data-grid without onRowClick = 0 (or a recorded exception per file)", "expected": "0 of 22", "status": "open", "evidence": "owner: claude-code-bsuite-main; measured 22:48 AWST at crm7 9308fc10: 17 of 22 DataGrid files missing onRowClick (ceiling holds); no crm7 PR yet; main lane"},
  {"id": "C9", "kind": "url", "step": "reports Name cell one line, on production", "check": "https://crm.crm7.app/reports templates grid: Name cell one line, Key its own column; per-user column width persists after reload", "expected": "true in both themes", "status": "open", "evidence": "operator 13:25 AWST overrode the 18:00 window for the mailbox P0 (live clients complaining): crm7 development->main d5499128 and BSU main a126ab8 promoted by the grok lane, applier dispatched per app, oauth functions redeployed; the D-156 Name/Key fix rode that promotion; column-width persistence still open; R80.4 stays at 18:00"},
  {"id": "J7", "kind": "gate", "step": "migration-history audit green by fixing the checker", "check": "scripts/audit-prod-migration-history.mjs strips comments before comparing and reads scripts/migration-collision-allowlist.txt; --self-test plants a real DDL difference and fails; the scheduled run is green; git log shows no migration renamed", "expected": "green; self-test fails on the plant; 0 renames", "status": "done", "evidence": "PR #2971 merged to development at 364fd281 (12:11 AWST) after an independent Fable review (PASS; md5 table, marker handling, string-literal `--` case and stale-allowlist failure reproduced through the CLI); the PR job \"Class C (version collision), file-only, no DB\" is green on every PR; the run dispatched on the branch (33713620651) is red on class B, which lists development’s four unpromoted parent migrations (20261106000000..20261109000000): awaiting promotion by design, not drift; the scheduled run on main turns green with the parent promotion. Zero migrations renamed or edited; 12 harmless duplicates banked {12, 279}."},
  {"id": "GITLINK", "kind": "gate", "step": "every parent gitlink sits on its app’s own main; the pointer workflow advances what is safe and refuses loudly", "check": "scripts/check-gitlink-in-app-main.mjs (PRs into main) asserts merge-base --is-ancestor <gitlink> <app origin/main> per app with a planted development-only SHA failing its self-test; scripts/advance-submodule-pointers.mjs compares against app main and stages safe advances even when another app is refused; braden advanced to its main tip through the fixed script", "expected": "gate present; conduit, R80.4, throughput read already current; only crm7 refused (its gitlink is a development tip) until crm7 promotes", "status": "done", "evidence": "PR #2969 merged (gate + advance script); reconciles merged; crm7 pointer #2978 (PI bank shrink) and #2987 (grok; auto-merged 16:03 with align red on the registry) and BSU pointer #2989 (bot PR taken over by the PI: registers regenerated, enforcer pack APPROVE); registry regenerations #2982 and #2989; rehearsal pg_net #2980. Rule recorded as precedent: a gitlink advance regenerates the generated registers in the same PR; follow-up: scripts/advance-submodule-pointers.mjs runs the generators itself"},
  {"id": "C10", "kind": "gate", "step": "CSS-class gate: a class used in src with no emitted utility fails", "check": "scripts/check-css-classes-emitted.mjs (planned) builds each app’s CSS with @tailwindcss/cli, diffs used utility tokens against emitted selectors, honours the theme-audit-ok marker, banks per-app {findings, files_scanned} (BSU 170) that only shrink; registered in theme-gates.sh as C10 and as a theme-conformance.yml job; self-test plants an unregistered class", "expected": "BSU 170 banked today, 0 after the four-line @theme fix (F8, main lane); five apps 0", "status": "done", "evidence": "gate PR #2975 merged to development at b04c952c (15:44 AWST) after an independent Fable review (FAIL on reason-less waivers + unscanned template-literal className) and a fix-forward lane: allowlist lines and theme-audit-ok markers now require a reason of 12+ characters or the run fails; template-literal className scanned; banks BSU 381, throughput 249, crm7 10 (two real bg-error-bg hits from the mail client, the main lane's cause 3 in crm7#2362), braden/conduit/R80.4 0; context `Every used class token has an emitted rule (C10)` path-filtered until C5 removes the filter; F8 (BSU#1106) merged to BSU development"},
  {"id": "C11", "kind": "pr", "step": "DESIGN.md at the parent root as the judgment layer every lane reads (audit §7 C11, added by #2965)", "check": "DESIGN.md exists, names @bsuite/theme as the mechanics layer and the theme check scripts by name, carries the known answers (error red never purple; no pure white or black; role tokens; one line per cell; no trailing margin in an autoHeight measure; no bare font name); every fan-out brief cites it; the hub repoints bsuite-brand-system after it lands", "expected": "merged to development; PR number reported to the audit PI", "status": "done", "evidence": "bsuite#2983 merged (~15:40 AWST): DESIGN.md at the parent root (291 lines, 22 gate scripts named, eleven known answers with incident + enforcement layer, J12 routing rule), AGENTS.md index row, theme README's pre-0.7.0 palette rows corrected, theme-gates.sh R80.3->R80.4 filter fix with a planted-fixture proof; @google/design.md lint 0 errors; BRIEF_COMMON_PHASE1.md cites it; audit PI told to repoint bsuite-brand-system"},
  {"id": "N1", "kind": "pr", "step": "refresh-to-update notice, package half (operator 10:48; D-160)", "check": "@bsuite/nav-core minor: useAppUpdateAvailable (running __BUILD_COMMIT__ vs /version.json, content-type asserted, any change = versionChanged, updateCheckBroken never silent), UpdateAvailableBanner (role=status, role tokens, Refresh through the dirty guard, critical non-dismissible), useUnsavedChanges, ./vite versionJsonPlugin (generateBundle + define, throws with no commit); tests with positive and negative controls; merged and published", "expected": "merged to development; published on the parent promotion", "status": "done", "evidence": "@bsuite/nav-core 1.3.0 merged to development at 1910dbd6 (#2974, 12:43 AWST): useAppUpdateAvailable (exact commit compare, content-type asserted before parse, kill switch and severity from system_notices), UpdateAvailableBanner (role=status, never reloads on its own), useUnsavedChanges/useRegisterDirty, `@bsuite/nav-core/vite` versionJsonPlugin; 193 tests; publishes to npm on the next parent promotion"},
  {"id": "N2", "kind": "count", "step": "refresh-to-update notice, app half: eleven mount points, four unconsented reloads removed, /version.json served as JSON on twelve domains", "check": "scripts/check-update-banner-mounted.mjs (planned) floor ≥1 mount per app (≥2 for R80.4); scripts/check-version-json-served.mjs (planned) = 200 + application/json on all twelve domains; grep window.location.reload( shows the four unconsented sites removed; Skew Protection state read for six projects", "expected": "11 mounts; 0 unconsented reloads; 12/12 JSON; Skew Protection on where the plan allows", "status": "open", "evidence": "throughput#465 merged to throughput development at 8c74f613 (16:48) on the enforcer's merge-ready verdict (D1-D8, D10 APPROVE; D9 pending); the PI's estate pass on d.ideas.crm7.app 16/16 clean (banner absent on the same commit, present on a changed one, no reload, no horizontal scroll, both themes, four widths); throughput promotion in the 18:00 window; crm7 (lane running, crm7#2368 lockfile merged 0288f9e0) and BSU (lane running, BSU#1116 merged bb84293f) adoptions next; conduit, braden, R80.4 follow; crm7 PROMOTED 21:18 AWST (#2374, merged by the shared identity with no qa-signed-off label and no enforcer verdict on the promotion; D-174 amended); crm.crm7.app and d.crm.crm7.app serve /version.json as JSON at 9308fc1; expected_missing 8 -> 6 in bsuite#3021; conduit main fb984bf2 carries the lockfile reach only; conduit/braden/R80.4 notice mounts next"},
  {"id": "DOD1", "kind": "gate", "step": "Definition of Done enforced across lanes (operator 15:56 AWST): nothing finishes or merges without an enforcer-run APPROVE", "check": "every lane report ends with gate_report.py status + files under lanes/dod/; the PI runs completion-enforcer before each merge; INDEX row = the register that tracks the work; D9 two-stage (merge-ready with a named closer, done after first real use)", "expected": "precedents recorded; retro enforcers on today's merges; fix-forward PRs for gaps", "status": "open", "evidence": "precedents nothing_finishes_or_merges_without_an_enforcer_run_dod_approve (T1) and dod_index_row_identity_and_the_two_stage_d9 (T2); BRIEF_COMMON_PHASE1.md DoD section; retro enforcer on #2939/braden#581 SEND_BACK (D7 restated on #2939, header fix braden#583 merged, Consumer lockfile reach unsigned-commit finding, D10 worktree residue cleaned); retro enforcer on the gate PRs pending; hub asked to add two registers to INDEXED_SILOS and to teach D10 worktrees; #3018 merged on its enforcer verdict; #3020 and #3021 enforcer dispatched 22:48 AWST; crm7#2374 merged 21:18 with NO verdict and the PI recorded it as held for 50 minutes after it had merged (a PI miss: a hold is re-read at every turn, never carried from memory)"},
  {"id": "DOD2", "kind": "gate", "step": "Bot review threads addressed before a merge (operator 16:21 AWST)", "check": "each lane lists every unresolved Copilot/Qodo/scanner/layout-lint thread with a disposition; the PI runs pr-bot-threads.sh before merging; an unresolved substantive thread blocks", "expected": "0 unresolved substantive threads on any PR the PI merges", "status": "open", "evidence": "measured 16:22 AWST: 20 unresolved Copilot threads across 9 open PRs, routed to their lanes; Qodo reviews are paused for the account on every crm7 and R80.4 PR (operator configuration item); precedent bot_review_threads_are_addressed_before_a_merge (T1); #3020 1/1 (D-167 whereLooked inconsistency, fixed c5b0d5e7 and resolved); #3017 2 threads still open (main lane)"},
  {"id": "FOLLOW", "kind": "list", "step": "follow-up rows from today's reviews and enforcer runs", "check": "each becomes a lane brief or a register row before Phase 1 closes", "expected": "listed here until dispatched", "status": "open", "evidence": "(1) C10: scan @bsuite package dists under node_modules with -R (symlinks), not only src; (2) C5: 7 required contexts without a failure-proof step; C7 check 4 (grants incl. TRUNCATE on banked RLS-no-policy tables) runs only on the live substrate; a check that a document's copy of a token value matches vars.css; (3) Consumer lockfile reach writes unsigned commits through the contents API and its own guard refuses them: give it a signed path; (4) data-grid refused-edit boundary 1.65:1 / 1.29:1 below WCAG 3:1 (package finding, main lane to file); (5) crm7 20261112000000 needs `-- rehearsal: guarded-no-op` (grok); (6) docs/plans/20260901-workflow-canvas-implementation plan currency (main); (7) Qodo paused for the account; (8) D-161 Reset to Default on the page-builder toolbar (PI lease); (24) a promotion can merge with its migrations unapplied (crm7#2374: 20261115/16/17 on main, not in the ledger; C3 not live): a post-promotion gate that compares the app main's migration versions with the ledger and stays red until the applier ran, on the pointer PR; (25) the auto-advance PR bsuite#3001 fails 8 checks because it regenerates nothing: teach advance-submodule-pointers.yml the regeneration list or retire it for manual pointer PRs; (26) C6 planted-case proof for test-skill-hub.sh (hub lane); (27) the D-160 notice for conduit, braden and R80.4 (N2 remainder); (28) a doc gate that diffs two-dot against a moved base enforces on other PRs' files (bsuite#3017: 'Docs declare kind, authority and evidence' red on #3014's doc) - the gate should diff merge-base...HEAD; (29) a pointer PR moves EVERY ratchet the app's main touched: C10 class tokens AND react-hook suppressions (bsuite#3021 SEND_BACK on the second) - the pointer runbook lists both --update steps; (30) six peer sessions alive at 23:40 with a second 'audit PI' identity messaging the hub lane at 23:38 - one PI per lane, roll call by SendMessage, the operator's 23:30 word makes claude-code-bsuite-pi the single PI; (31) wire `resolve` objects onto this contract's json block for the hub's loop-gate (hub 21d0112: evidence/baseline/pr_merged/ci_run/ci_check/key/url; C3, C7, C9 resolve against PRODUCTION reads, never pr_merged), then arm with scripts/loop-arm.py; (32) the required gitleaks job (semgrep-sast.yml) fetches EVERY branch (+refs/heads/*, fetch-depth 0) and scans all 3,824 commits, so a key-shaped fixture string on ANY feature branch (bsuite#3027 f7ac5285:529, rule airtable-api-key) turned the check red on every open PR at 01:03 - scope the scan to the PR's own range (origin/<base>..HEAD) or the PR branch, keep a scheduled full-history scan separate, and add a self-test that plants a fixture on a side branch and expects the PR scan NOT to fail; (33) C3 OUTPUT: the watchdog's first check-in names document-retention-sweep-daily (16 failed, 0 ok since 2026-08-19) and sync-award-rates-weekly (2 failed, 0 ok since 2026-08-23), both refusing to POST to a null endpoint because their four vault entries were never seeded (document_retention_sweep_url/_secret, sync_award_rates_url/_token) - an OPERATOR console step (secrets cannot ride a migration): one-shot runbook filed as a bsuite issue labelled external-blocked-operator; the observer comments on #3015 on every unhealthy check-in until both succeed"}
]
```

## Rows (the PI ticks with the evidence pointer, never on a code trace)

| Id | Owner | PR | Status | Evidence |
|---|---|---|---|---|
| A1 | hub lane (audit PI) | | **done** | hub delivered: ~/.agents/hooks/loop-gate.py wired as a Stop hook (~/.claude/settings.json:71); planted cases loop-gate.must-block-open-item / must-block-contract-missing / must-block-not-in-payload and must-not-block-resolved / must-not-block-not-armed all pass (test-hook-fixtures.sh: 17 passed, 0 failed, 22:48 AWST); kill switch ~/.agents/state/gates-off absent |
| A2 | pi | | open | four-part layout used from 12:00; enforcement rides on the hub loop gate; operator 23:45 AWST (23:30): 'set a recurring loop until all audit findings have been addressed and production holds all corrections; drive all agents forward as PI' - cron 7601e708 every 7 min replaces 316eb900 with a widened prompt (crm7 live chain, hub-lane PI duties, Phase 1 open rows, section-7 Phases 2-5, D-140..D-145); the overnight stalls were HOOK-generated: ~/.agents/hooks/pre-destructive-git.py returned permissionDecision ask, which overrides bypassPermissions; hub 4a5eaa9 makes it snapshot+log+allow (planted case: dirty repo + reset --hard -> allow, presnap ref written; ask under WORK_AT_RISK_ASK=1) |
| A3 | hub lane (audit PI) | | **done** | loop line printed on every PI turn; the hook half is the loop-gate's must-block-not-in-payload fixture (arming record absent from the Stop payload blocks), passing 22:48 AWST |
| A4 | hub lane (audit PI) | | open | advisory, transcript-sourced (ruling 9); still advisory at 22:48 AWST (ruling 9); no fixture; stays open |
| A5 | pi | #2970 | open | 160/160 judged; registered_at on every row; F-verdict banked at 0 |
| C1 | pi | #2968 | **done** | merged c18efaaf after review + fix-forward; advisory workflow; tool-written baselines |
| C2 | pi | #2970 | **done** | merged 41b12223 after 7 review issues fixed forward |
| C3 | db-worker (crm7 clone + parent observer) | #2985 ✓, crm7#2363 ✓ | **done** | enforcer merge-ready 17:40 (blocker reproduced pre-fix on real pg_cron 1.6.4, closed by a real worker; pgTAP 20/20, 11 fail under sabotage); merged 2ad25440 + f2a6744c; D9 closes on the first production check-in after the crm7 applier; F1/F2 in FOLLOW; NOT LIVE on production at 22:48 AWST: crm7 20261117000000 (the watchdog function, table, manifest and its 15-minute job) is on crm7 main 9308fc10 but absent from supabase_migrations.schema_migrations; to_regclass('public.cron_watchdog_checkins') and pg_cron_watchdog_check() are NULL on production, so the parent observer has nothing to read. Route: bsuite#3021 (crm7 gitlink) -> parent promotion -> supabase-migrate.yml -f submodule=crm7 -> effect check. Status flipped from done to open until that effect is read; LIVE on production at 00:47 AWST 2026-09-04: parent promotions #3022 (crm7 pointer 38798155) and #3029 (applier ledger INSERT on stdin, #3028) merged; applier run 33780540784 applied and recorded 20261115/20261116/20261117 (the first run, 33778653772, applied 20261115 and failed the ledger INSERT on psql's -c non-interpolation - fixed forward); read-only effect: schema_migrations rows 1/1/1, cron_watchdog_checkins + pg_cron_watchdog_check() + cron_job_manifest exist, cron.job carries cron-watchdog-every-15m (active), communications_recipient_type_check includes client. FIRST CHECK-IN 00:52:00 AWST 2026-09-04 (cron.job_run_details: succeeded): healthy=false, 25 of 27 jobs healthy, unhealthy named in the row (document-retention-sweep-daily last_run_failed - the audit's first proof case - and one more); the switch reports on EVERY run, so a silent estate now shows as a missing row. D9 closer: the parent observer (cron-watchdog-observer.yml) reading a check-in and opening an issue on the unhealthy set; D9 CLOSED 00:55 AWST: observer run 33781273309 (cron-watchdog-observer.yml, dispatched on main) read the production check-in and posted to bsuite#3015: unhealthy - document-retention-sweep-daily (last_run_failed), sync-award-rates-weekly (last_run_failed), 4 http-layer failures since the previous check-in; the run exits 1 by design. The switch, the check-in and the reader are all live |
| C4 | hub lane (audit PI) | | **done** | hub delivered: ~/.agents/hooks/dod-gate.py wired as a Stop hook (~/.claude/settings.json:62); planted cases dod-gate.must-block (a .tsx edit + done-claim with no gate run) and must-not-block-declared (ui_touched: false) / must-not-block-backend / must-not-block-no-claim all pass (test-hook-fixtures.sh 17/17, 22:48 AWST); kill switch absent |
| C5 | pi worker | #2988, #2997 | **done** | merged 3f11ddf0; post-merge append done 17:58: grant-lint + the C10 class-token gate required after reporting on #2996; dev 31 / main 34; dumps refreshed ae3bda8e; meta-gate 65 contexts exit 0 |
| C6 | hub lane (audit PI) | | open | item 4 amendments adopted (fb57b988); ~/.agents/scripts/test-skill-hub.sh: 38 passed, 0 failed at 22:48 AWST (content checks included); the planted-case proof (a file with an empty name fails the run) is owed by the hub lane |
| C7 | db-worker (braden clone + parent gate) | braden#581 ✓, #583 ✓, #2984 ✓, #585 ✓, #587/#588 ✓, #589/#590 ✓, #591/#592 ✓, #3004 ✓, #3010 ✓, #3019 ✓ | **done** | gate merged 6dc88b43; the REVOKE reached production on the THIRD version (20261119221500) after two hand-applied crm7 migrations shadowed 20261118000000 and 20261119000000 (D-172); applier run 33764732242 at 22:04: 1 applied; EFFECT 22:06: has_function_privilege(anon, is_developer_admin(), EXECUTE) = false, authenticated/service_role true, no PUBLIC entry in proacl; the allowlist line removed in #2998 |
| C8 | main | | open | directive 01d27eda; C2 ceiling banked at 17 of 22; measured 22:48 AWST at crm7 9308fc10: 17 of 22 DataGrid files missing onRowClick (ceiling holds); no crm7 PR yet; main lane |
| C9 | main, promotion pi | | open | promoted early under the operator's 13:25 override (crm7 main d5499128); column-width persistence still open |
| C10 | gate pi, F8 main | #2975 | **done** | merged b04c952c after review + fix-forward; F8 = BSU#1106 merged |
| C11 | design-sheriff worker | #2983 | **done** | DESIGN.md merged; hub to repoint bsuite-brand-system |
| J7 | pi | #2971 | **done** | merged 364fd281; Fable review PASS; Class C green on every PR |
| GITLINK | pi | #2969, #2978, #2987, #2989, #2998 | **done** | pointers on app mains; registers regenerated in-PR (precedent); 18:00 window: R80.4 d46fd8db, throughput 189fb127, braden a7c0fa0b (#2998) |
| N1 | pi | #2974 | **done** | nav-core 1.3.0 merged 1910dbd6; publishes on the next parent promotion |
| N2 | pi + main + app workers | #2976 ✓, throughput#465 ✓, throughput#467 ✓, BSU#1119 ✓, crm7#2373 | open | throughput PROMOTED 18:01; BSU#1119 merged 18:33 (enforcer merge-ready after the build gate was wired into Vercel's script and a counting test added; d.suite pass 16/16), promotion BSU#1120 open; crm7#2373 enforcer running, promotion crm7#2374 open; conduit/braden/R80.4 next per R10; crm7 PROMOTED 21:18 AWST (#2374, merged by the shared identity with no qa-signed-off label and no enforcer verdict on the promotion; D-174 amended); crm.crm7.app and d.crm.crm7.app serve /version.json as JSON at 9308fc1; expected_missing 8 -> 6 in bsuite#3021; conduit main fb984bf2 carries the lockfile reach only; conduit/braden/R80.4 notice mounts next |
| DOD1 | pi + every lane | | open | enforcer runs recorded in lanes/dod/: #2990 SEND_BACK→fixed→merged 002053bc; C3 merge-ready→merged; throughput#465 merge-ready→merged; BSU#1117 SEND_BACK 17:20 (six gaps, core stands); R80.4#301 SEND_BACK 17:37 (two gaps, both closed by 17:45, flip pending); retro gates on C5/C7/lockfiles/grok; verdict recording unfrozen by #2990 (first writeback #2995); #3018 merged on its enforcer verdict; #3020 and #3021 enforcer dispatched 22:48 AWST; crm7#2374 merged 21:18 with NO verdict and the PI recorded it as held for 50 minutes after it had merged (a PI miss: a hold is re-read at every turn, never carried from memory) |
| DOD2 | pi + every lane | | open | bot threads addressed before merge (operator 16:21): #2990 1/1, #2994 2/2, R80.4#302 2/2 + #301 3/3, C3 4/4 + 1/1, BSU#1117 3/3 + 2/2, crm7#2372 2/2 + 2/2, braden#585 1/1 and throughput#467 1/1 (both answered by measurement), #2998 2/2, #3003 2/2, #3000 2/2; open: throughput#466 2; #3020 1/1 (D-167 whereLooked inconsistency, fixed c5b0d5e7 and resolved); #3017 2 threads still open (main lane) |
| FOLLOW | pi | | open | (1) generator: recompute sibling_count on regeneration (148 stale on 146 rows); (2) C3 F1: assert unhealthy_jobs holds no cron-watchdog-every-15m by name; F2 suite 91 header; (3) AppShell root unbounded (D-167, @bsuite/ui class, D8 per consumer); (4) R80.4 Jodie gate tells a signed-in person to sign in (D-166); (5) plan-currency marker on 20260901-workflow-canvas-implementation-v1.00A.md (red on development since #2948); (6) lockfile pin reach clears on gitlink advances (throughput/braden/R80.4 moved 18:00; conduit next); (7) estate probe: wait for the subject, never networkidle; theme via the app's own key and hash the captures; assert on DOM nodes, never innerText (clipped content missing: 16,662 vs 21,327 on R80.4); (8) results-tab index rows re-modelled to cards (D-168, main lane, after #2995); (9) floors guard: typescript must be resolvable from scripts/ (NODE_PATH to the pnpm store locally); (10) app-repo PR CI runs the cross-scope migration collision check (D-170); (11) GitHub App replaces the PAT: 2 writers first, 61 readers next (D-169); (12) the vendored route inventory inside BSU (src/data/route-inventory.json) needs a two-phase regeneration when other pointers move: a BSU commit re-vendors it, then the BSU gitlink advances (check-inventory red on #3003 by design); (13) estate-align maps a register row to the index by its surface text: platform rows must name 'every app'/'estate-wide' or they read as index holes (D-170 was); (14) a doc added to a directory needs that directory's generated index in the same PR (generate-docs-directory-index.mjs); (15) enforcers on opus die on the session limit: re-dispatch on sonnet and let the partial evidence file be finished by the successor; (16) D-171 mail-merge built out (email lane); (17) the applier refuses to treat a nameless, statement-less ledger row as applied without an allowlist entry, and every migration header names an EFFECT check the applier runs after 'Verify recorded' (D-172; the nontx producer fixed in #3012); (18) braden production policies vs migration files reconciled forward (D-173); (19) the version survey includes every OPEN PR across the 7 repos, not only branch tips (crm7#2377's 20261119000000 was open when the PI chose it); (20) D-174: the two un-gated crm7 merges ruled on before crm7 promotes (ruled 21:03: #2375 SEND_BACK on a false-complete billing path, #2377 safe but untested); (21) doc-classification.yml's docs/**/*.md pathspec never matches root-level docs/*.md (the job enforced on nothing for the layout standard); (22) D-169: PAT fallback removed from the two writers now that the App mints tokens, then the 61 readers, then the PAT revoked; (23) the collision poster (#3017) runs on a schedule on the default branch only: live after a promotion, up to 3 h late; a pull_request/repository_dispatch trigger is the next step; (24) a promotion can merge with its migrations unapplied (crm7#2374: 20261115/16/17 on main, not in the ledger; C3 not live): a post-promotion gate that compares the app main's migration versions with the ledger and stays red until the applier ran, on the pointer PR; (25) the auto-advance PR bsuite#3001 fails 8 checks because it regenerates nothing: teach advance-submodule-pointers.yml the regeneration list or retire it for manual pointer PRs; (26) C6 planted-case proof for test-skill-hub.sh (hub lane); (27) the D-160 notice for conduit, braden and R80.4 (N2 remainder); (28) a doc gate that diffs two-dot against a moved base enforces on other PRs' files (bsuite#3017: 'Docs declare kind, authority and evidence' red on #3014's doc) - the gate should diff merge-base...HEAD; (29) a pointer PR moves EVERY ratchet the app's main touched: C10 class tokens AND react-hook suppressions (bsuite#3021 SEND_BACK on the second) - the pointer runbook lists both --update steps; (30) six peer sessions alive at 23:40 with a second 'audit PI' identity messaging the hub lane at 23:38 - one PI per lane, roll call by SendMessage, the operator's 23:30 word makes claude-code-bsuite-pi the single PI; (31) wire `resolve` objects onto this contract's json block for the hub's loop-gate (hub 21d0112: evidence/baseline/pr_merged/ci_run/ci_check/key/url; C3, C7, C9 resolve against PRODUCTION reads, never pr_merged), then arm with scripts/loop-arm.py; (32) the required gitleaks job (semgrep-sast.yml) fetches EVERY branch (+refs/heads/*, fetch-depth 0) and scans all 3,824 commits, so a key-shaped fixture string on ANY feature branch (bsuite#3027 f7ac5285:529, rule airtable-api-key) turned the check red on every open PR at 01:03 - scope the scan to the PR's own range (origin/<base>..HEAD) or the PR branch, keep a scheduled full-history scan separate, and add a self-test that plants a fixture on a side branch and expects the PR scan NOT to fail; (33) C3 OUTPUT: the watchdog's first check-in names document-retention-sweep-daily (16 failed, 0 ok since 2026-08-19) and sync-award-rates-weekly (2 failed, 0 ok since 2026-08-23), both refusing to POST to a null endpoint because their four vault entries were never seeded (document_retention_sweep_url/_secret, sync_award_rates_url/_token) - an OPERATOR console step (secrets cannot ride a migration): one-shot runbook filed as a bsuite issue labelled external-blocked-operator; the observer comments on #3015 on every unhealthy check-in until both succeed |

## Stopping conditions

- **Success:** every row `done` with evidence, the phase red-teamed by two independent models, `gate_report.py` APPROVE with `ui_touched` and `ephemera` declared, promotion to `main` scheduled by the PI.
- **Blocked:** a row whose closure needs an external party is marked `blocked` with the issue number and the one-shot runbook; the phase does not wait for it.
- **Never:** a row ticked on a code trace; a baseline reset upward; a migration renamed; a write to FutureBuild Academy data.

## Tasks

- [ ] A1 to A5 shipped with evidence (A1, A3 done by the hub's hook fixtures; A2 on every PI turn; A4 advisory; A5 160/160 judged)
- [ ] C1 to C10 shipped with evidence (C1, C2, C3, C4, C5, C7, C10 done with production evidence; C6/C8/C9 per their rows)
- [x] J7 shipped with evidence (#2971, 364fd281)
- [ ] Phase red-teamed by two independent models; `gate_report.py` APPROVE recorded here

**Operator amendment 13:25 AWST (recorded 15:05):** "You MUST promot to production when its ready. this is a live issue and clients are complaining." A defect live clients are hitting promotes as soon as it is ready and green; everything else still waits for the refresh notice or an after-hours slot (precedent `live_clients_stage_to_development_and_notify_before_refresh`, amended). Migrations run through the parent applier on each app's promotion, never by hand (precedent `migrate_on_each_repo_prod_promote`).

**18:00 window (recorded 18:10):** R80.4#301 promoted 17:51 (main d46fd8db, Vercel production READY on that SHA, enforcer merge-ready, D9 pending on FutureBuild's next real calculation); throughput#467 -> main 189fb127 at 18:01; braden#585 -> main a7c0fa0b at 18:02; root applier run 33741911943 applied 20261106..20261109 (4 applied, 8 already recorded); gitlink PR #2998; parent promotion + braden applier follow. Both promotion PRs carried one Copilot thread each, answered by measurement (live rewrite behaviour; the migration applied on PostgreSQL 17) before merge.

**Parent promoted 15:28 AWST:** bsuite#2939 merged to main at c2f3aa2e with `--merge` and the PI's qa-signed-off (evidence on the PR); six gitlinks on app mains; nav-core 1.3.0 published; the parent-scope applier dispatch is held to the 18:00 window because 20261109 replaces a payroll function and no app main calls the new RPCs yet.
