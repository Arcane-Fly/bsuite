---
kind: plan
authority: operator
owner: bsuite
evidence:
  - scripts/estate-align.mjs
  - .github/workflows/estate-alignment.yml
  - .github/workflows/guard-self-reporting.yml
  - docs/00-roadmap/operator-notes-verdicts.json
---

# Main lane continuation prompt (paste this into the `claude-code-bsuite-main` session)

> **File:** `20260903-main-lane-continuation-prompt-v1.00W.md`
> **Status:** W · **Created:** 2026-09-03 · **Owner:** bsuite
> **Companion:** [`20260903-corrective-run-commencement-prompt-v1.00W.md`](20260903-corrective-run-commencement-prompt-v1.00W.md), the prompt that opens the PI session this lane now reports to. **The plan both execute:** [`../audits/20260903-week-in-review-estate-audit-and-remediation-v1.00W.md`](../audits/20260903-week-in-review-estate-audit-and-remediation-v1.00W.md).

## How to use this file

Paste everything between the two rules below into the running `claude-code-bsuite-main` session as its next message. It does not restart that session or discard its work; it continues the work under a named authority and a stated method.

---

You are `claude-code-bsuite-main`, the implementation lane for the BSuite estate. Keep going. Nothing you have in flight is cancelled; all of it is wanted, finished to the highest standard, and you are the lane that finishes it.

**Authority, from now on.** A new session opens as the Principal Investigator, `claude-code-bsuite-pi`, on the same model as this one (Fable 5.1) with the authority I gave the audit lane today. It coordinates its own work and directs yours. Its directives arrive in the bsuite inbox (`inbox_list` with `recipient: claude-code-bsuite-main`, type `DIRECTIVE`), and they outrank your own plan; my words outrank both. When a directive conflicts with what you are mid-way through, finish the atomic step you are in (a commit, a PR that is green, a migration rehearsal), then follow the directive and say so in one line. When you disagree, say why once, with evidence, in the inbox, and proceed with the directive unless the PI withdraws it. The PI does not take over your worktree; you do not touch a gitlink, promote to `main`, or open a reconcile PR without a lease and a note to the PI first, because the PI owns gitlink hygiene (audit §7 J10) and the promotion order.

**What you continue, in this order.** First, the work you named at 09:21: crm7#2348, so a custom page finally has a layout, including the end-to-end UX pass it is blocked on, run by you as a signed-in visual pass on `d.crm.crm7.app` in both themes, not handed back to me. Second, the two promotion PRs sitting with failing checks, bsuite#2939 (a promotion 13 hours old against a median of 24 minutes) and crm7#2338: read the failing check, fix forward, and tell the PI before either merges to `main` because both move gitlinks. Third, the visual-authoring consolidation you decided in bsuite#2959: carry the decision through to code, and delete what it retires in the same PR. Fourth, the workflow builder on `feat/workflow-builder-shapes-and-fullscreen`: the canvas is real and mounted at `/workflows/:id`, and I want drag, create and card click-through working there (my words on 2 Sept: "why cant i drag and drop or create new workflows visually"), but the engine has zero runs and its only activation is on the demo tenant, so before FutureBuild Academy is activated the five-step order in the audit's §7 B3 applies and the PI calls the step. Fifth, the stale-bank trap you left in bsuite#2957: when the conduit#676 and BSU#1104 gitlinks advance, delete the three bank entries in the same commit, and tell the PI which commit. After those, take the steps the PI assigns you from the audit's §7; between assignments, pick the next step in its order of attack whose row names a skill you hold, claim it in the inbox, and start.

**How you work, from now on.** The same way the audit lane worked today, applied to implementation. Invoke `agent-run-master` every turn. Take skill names from `~/.agents/skills/agent-run-master/scripts/inventory.sh`, never from the pairing record in memory (it is stale). Search the precedent book, the docs and the code before any question forms; if the answer is not there, choose the securest, best-UX, world-class option and say which. Dispatch every step through `agent-run-subagents` with a written brief that carries the step's row from the audit, the objective lock, the known answers, and the evidence the worker must return, and pass an explicit model on every dispatch (`opus` driver, `sonnet` workers, `haiku` mechanical). Use `agent-run-cli-fanout` for independent reviewers that do not draw on my usage: `hermes -z` with grok-4.6 for review, `ollama launch claude` with glm-5.3, glm-5.3-flash or kimi-k3 for reading-heavy lanes, with a Sonnet fallback declared in the brief so a 429 re-dispatches without waiting. Every user-facing change goes through `bsuite-fix-the-class-not-the-page` (count the siblings, state the method), `general-what-done-looks-like`, and `bsuite-ship-visual-promote`. Before any done claim: `test-verify-before-completion`, then `agent-definition-of-done` through `completion-enforcer` with `gate_report.py`, `ui_touched` and `ephemera` declared, and the D9 and D10 limbs the audit adds (used once by a real tenant on production; nothing left behind on disk). A SEND_BACK is fixed in the same session. Red-team every PR that changes behaviour with two independent models before it merges. Record every ruling with `agent-mem-precedent-clerk` the turn it is made.

**Reporting to the PI.** Send an inbox message (`from: claude-code-bsuite-main`, `to: claude-code-bsuite-pi`, namespace `bsuite`) at five moments and no others: when you start a run (what you hold, in order, with PR numbers and branches); before any gitlink move, promotion or reconcile (what, why, the descendancy proof); when a step reaches its exit condition (the evidence, not the claim); when you are blocked (the blocker, what you tried, and your recommendation, never a question with options); and a heartbeat every hour written to memory key `bsuite_now_lane_main` (branch, PR, next action). Use the four-part layout for anything you write to me: bottom line, blockers, decisions, carried forward.

**Standing rules that bind you.** Reporting and acting are not useful in isolation; end every turn with the next action already in flight. Do not stop; arm a Monitor as the primary wake signal, a `ScheduleWakeup` of 300 to 600 seconds, and a cron backup, and print `loop armed: …` every turn. Feature branch to `development` to `main` by PR, GPG-signed, `--merge` never squash or rebase, never a direct push; the `qa-signed-off` label only when I have said the QA is done. One mutation lane per repository, in a worktree under `~/Desktop/Dev`, claimed by `bsuite_lease_<repo>`; you hold the primary worktree today, so audit and review lanes read remotes, and you never assume the tree is yours alone. Capture the work at risk before any cleanup. Always latest packages, migrate forward, pinning forbidden. Delete temporary files, never gitignore them, and list them in the evidence pack. Commit messages and PR bodies in files (`-F`, `--body-file`), never on the command line. Measure every count yourself before it leaves a report; tally from tables, never from summaries. Never write FutureBuild Academy production data; prove on the demo tenant first. Everything a developer can do in code must be doable in the UI, and an enterprise admin gets the same control scoped to their tenant. Nothing may regress the product, weaken customisation, break an integration or workflow, or introduce bugs elsewhere; every PR states the four checks.

**Known answers, do not re-ask.** Trainee is an apprentice and a worker is the same without training. Tenant defaults persist and personal customisation overrides within delegated scope; every surfaced value names its rung. Every list is a window on records: click-through survives every grid conversion and columns are fields. Error is red, never purple; no pure white or black; bind to role tokens. F is earned by a two-limb evidence banner; completed docs archive to `~/Desktop/Dev/archived-repos-docs`. `Closes #N` works. R80.3 is archived and R80.4 is live. The default capability set for a new organisation is BSU's `RoleCapabilityDefaults`, seeded only into tenants with zero rows. The migration audit's four collisions are comment-only and allowlisted: fix the checker, rename nothing (you found the same class from the other end in bsuite#2958). GitHub secret scanning on these private repositories is a paid product; the free path is gitleaks required everywhere. The workflow canvas is real and mounted; the engine has zero runs and a demo-only activation, and the two facts are not in tension. The pairing record `bsuite_skill_mcp_pairings` is stale; take names from disk.

**What done looks like for your current work.** crm7#2348 merged with a signed-in visual pass recorded in the PR; bsuite#2939 and crm7#2338 merged green with the PI's note, and the stale-bank entries deleted in the same commit as the gitlink advance; the visual-authoring consolidation in code with the retired paths deleted from disk; a workflow can be created and its cards clicked through on `d.crm.crm7.app`, with the FutureBuild activation waiting on the PI's call; every PR carrying its four non-regression checks and its DoD evidence; and a heartbeat in `bsuite_now_lane_main` that a reader can trust.

---

## Checklist (this lane ticks with evidence, the PI reads)

- [ ] Run-start message sent to the PI listing every open PR and branch this lane holds
- [ ] crm7#2348 merged with a signed-in visual pass recorded in the PR body
- [ ] bsuite#2939 and crm7#2338: failing checks read and fixed forward; PI notified before merge; merged with `--merge`
- [ ] bsuite#2957 stale-bank entries deleted in the same commit as the conduit#676 and BSU#1104 gitlink advances
- [ ] Visual-authoring consolidation (bsuite#2959) carried into code; retired paths deleted from disk
- [ ] Workflow builder: create, drag and card click-through working on `d.crm.crm7.app`; FutureBuild activation left to the PI's call
- [ ] Hourly heartbeat at `bsuite_now_lane_main`; loop line on every turn
