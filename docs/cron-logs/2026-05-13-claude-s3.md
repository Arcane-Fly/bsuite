# Claude Code Scheduled — Overnight Cron Session Log

**Date:** 2026-05-13  
**Session:** s3 (supersedes s2 PR #912)  
**Agent:** claude-code-scheduled  
**Operator:** OFFLINE since 2026-05-06T13:05Z  

---

## Infrastructure Status

| Service | Status | Notes |
|---|---|---|
| Memory API (`qig-memory-api.vercel.app`) | ❌ BLOCKED | Host not in allowlist — presence/inbox/workqueue unreachable |
| `/ship-all-apps` | ❌ Not invocable | Slash command not available from cloud cron |
| Submodule code access | ❌ | crm7/conduit/etc. require operator-local session |
| GitHub MCP tools | ✅ | All PR/issue operations available |

---

## Actions Taken

### PRs Merged (§20 autonomous — red-team + smoke + no-orphan + no-dead-code all green)

| PR | Title | Merge SHA | Notes |
|---|---|---|---|
| #885 | docs(auth): codify VITE_APP_URL + Stripe pk env requirements | `c8ca668` | Docs-only. AGENTS.md + AUTH_CANONICAL.md + auth-map-reference |
| #887 | chore(auth/env): resolve OAuth redirect URI from VITE_APP_URL | `500f1eb` | packages/auth oauth-client.ts updated + 4 new tests |
| #875 | chore(ci): enforce AI Gateway compliance in root CI | `73551ab` | Adds check-ai-gateway-compliance.mjs + CI step |
| #886 | feat(rls): three-tier branding authorization (Developer Portal scope) | `e916271` | can_manage_tenant_branding() + branding RLS rebuilt + AUTH_CANONICAL.md |

### PRs Blocked (operator action required)

| PR | Title | Block Reason |
|---|---|---|
| #877 | refactor(cron-a): routing matrix + SLA tracker | `dirty` — needs rebase; conflicts with #882 `packages/jodie/` |
| #882 | feat(jodie): GitHub App webhook + token cache | `dirty` — needs rebase; `packages/jodie/` conflicts with #877 |
| #888 | Parent-level Autonoma adoption audit workflow | CI `verify-autonoma-adoption` failing |
| #890 | Stripe FDW for new Stripe read paths | AUTH_CANONICAL.md conflict after #886; also needs Vault `stripe_api_key` secret |
| #896 | Codify apprentice_rate_configs migration provenance | Empty diff — crm7 is a submodule; files must be committed in crm7 repo |

### PRs Closed

- **#896**: Closed as no-op (empty diff — Copilot cannot commit to crm7 submodule via bsuite PR)
- **#912**: Superseded by this s3 session log (closing below)

### Issues Updated

| Issue | Action | Notes |
|---|---|---|
| #609 | ✅ Closed (completed) | Resolved by #886 — three-tier branding RLS now live |
| #607 | 🟡 Comment — partial | Docs/code done; operator must set VITE_APP_URL in Vercel for all 6 apps |
| #550 | 🟡 Comment — partial | CI gate (#875) landed; submodule LLM migration still needed |

---

## Red-Team Evidence

### #885 (docs)
- Output-equivalence (§9.1): N/A — docs only  
- Visual-equivalence (§9.2): N/A  
- Self-report: Correct canonical domain map matches CLAUDE.md. No security concerns.

### #887 (OAuth redirect URI)
- Output-equivalence (§9.1): 4 new regression tests pass in CI  
- Self-report: `new URL()` validation + `window.location.origin` fallback. Security: env var is build-time, not runtime user input. Safe.

### #875 (AI Gateway CI)
- Output-equivalence (§9.1): Script scans git ls-files; CI build-and-test passed  
- Self-report: Heuristic string-based detection (not AST). `CALL_WINDOW_SIZE=1200` is documented. May false-positive on legitimate calls without exact metadata keys in bsuite parent repo; actual risk is low since parent repo has no AI SDK call sites.

### #886 (Branding RLS)
- Output-equivalence (§9.1): Migration adds can_manage_tenant_branding() + rebuilds policies  
- Self-report: Recursive CTE depth capped at 16. STABLE + SECURITY DEFINER is correct (auth.uid() is fixed per statement). Indexes added. Note: assumes `profiles.platform_role` column exists (pre-existing schema requirement).

---

## Operator Actions Required (Priority Order)

1. **Set Vercel env vars** (`VITE_APP_URL` + `VITE_STRIPE_PUBLISHABLE_KEY`) for all 6 apps across Production/Preview/Dev — resolves #607
2. **Consolidate #877 + #882** `packages/jodie/` into single coherent package, then rebase both onto `development`
3. **Fix #888 CI** — diagnose `verify-autonoma-adoption` failure, then undraft + merge
4. **Rebase #890** onto `development` (AUTH_CANONICAL.md conflict after #886), add Vault `stripe_api_key` secret, then merge
5. **Add crm7 migration files** for apprentice_rate_configs directly in crm7 repo (resolves #865) — #896 was a no-op
6. **Add memory API cron host to allowlist** — unblocks steps 1–4 of protocol in future sessions

---

## Evidence

- [ ] Output-equivalence (§9.1): N/A — cron coordination log only  
- [ ] Visual-equivalence (§9.2): N/A  
- [ ] Self-report: Memory API blocked; all actions via GitHub MCP only; no /ship-all-apps available  
- [ ] Tests run: CI green on all 4 merged PRs (verified via check_runs before merge)  
- [ ] Live verify: Merge SHAs: c8ca668 / 500f1eb / 73551ab / e916271  
